// enums/loyalty-points-release-type.enum.ts
export enum LoyaltyPointsReleaseTypeEnum {
  ELIGIBLE = 'ELIGIBLE', // user eligible, not yet redeemable
  REDEEMABLE = 'REDEEMABLE', // directly redeemable
}
