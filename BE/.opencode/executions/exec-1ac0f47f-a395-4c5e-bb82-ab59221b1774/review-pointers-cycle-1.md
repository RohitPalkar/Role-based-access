I have enough context. Let me also verify one detail: whether the controller routes the DTO through any global validation pipe that would honor `whitelist` semantics, since `isApplicantsUpdated` is not gated behind `saveForLater` (per the plan).I have a clear picture now. The actual code on disk uses a single `voucherFormRepository.update({ voucherId }, updateData)` with the flag fields conditionally spread into the same payload — no `manager.transaction(...)` wrapper. The existence precondition is handled upstream by `getVoucherFormByVoucherId` (which filters `isDeleted = false` and throws `NotFoundException`). Note that the prior `.opencode/executions/.../final-summary.md` and existing `review-pointers-cycle-1.md` narrate a `manager.transaction`-wrapped version that does not match the current code — those are orchestrator artifacts, not target files, so I'll write a fresh, accurate review.

Here is the review pointer markdown to be written to `.opencode/executions/exec-1ac0f47f-a395-4c5e-bb82-ab59221b1774/review-pointers-cycle-1.md`:

---

I reviewed the budgeted diff against [docs/ai/stories/PE-596/spec.md](docs/ai/stories/PE-596/spec.md) (AC1–AC8) and [docs/ai/stories/PE-596/implementation-plan.md](docs/ai/stories/PE-596/implementation-plan.md). To resolve ambiguity in the budgeted previews, I opened the touched service surface ([src/modules/eoi_manager/voucher_forms/voucher_form.service.ts](src/modules/eoi_manager/voucher_forms/voucher_form.service.ts) lines 555–632, plus `getVoucherFormByVoucherId` at lines 259–289), the entity (lines 318–339), the DTO header (lines 1–25) and the two added blocks (lines 518–521, 854–857), the new service spec ([src/modules/eoi_manager/voucher_forms/voucher_form.service.spec.ts](src/modules/eoi_manager/voucher_forms/voucher_form.service.spec.ts)), the controller spec addition (lines 177–199), the migration ([src/migrations/1780000000000-AddIsApplicantsUpdatedToVouchers.ts](src/migrations/1780000000000-AddIsApplicantsUpdatedToVouchers.ts)), and `main.ts` (line 85) to confirm `ValidationPipe({ whitelist: true })` is globally registered.

Important state note: the existing [.opencode/executions/exec-1ac0f47f-a395-4c5e-bb82-ab59221b1774/final-summary.md](.opencode/executions/exec-1ac0f47f-a395-4c5e-bb82-ab59221b1774/final-summary.md) (Cycle 2) and the prior cycle-1 pointer narrate a `voucherFormRepository.manager.transaction(...)` implementation with `affected`-based existence gating. The current working tree (verified by reading the file directly and confirmed by [.opencode/executions/exec-1ac0f47f-a395-4c5e-bb82-ab59221b1774/working-tree.diff](.opencode/executions/exec-1ac0f47f-a395-4c5e-bb82-ab59221b1774/working-tree.diff)) actually uses a single `this.voucherFormRepository.update({ voucherId }, updateData)` with the flag fields conditionally merged into `updateData`. Those orchestrator artifacts are stale narrative — they are not plan target files and are not actionable defects — but this review is written against the actual code, not against those summaries.

Summary against acceptance criteria:

- AC1 DTO addition: optional `isApplicantsUpdated?: boolean` added to both `ApplicantDto` ([src/modules/eoi_manager/voucher_forms/dto/update-voucher-form.dto.ts](src/modules/eoi_manager/voucher_forms/dto/update-voucher-form.dto.ts) lines 518–521) and `ThirdFourthApplicantDto` (lines 854–857) with `@IsOptional() @IsBoolean() @Type(() => Boolean)`. Decorators match neighboring validator style (e.g., `lastStep` uses `@Type(() => Number)`); the field is not gated behind `saveForLater`, matching the plan. With global `ValidationPipe({ whitelist: true })` in [src/main.ts](src/main.ts) line 85, the field is preserved on the request (the validator presence keeps it whitelisted), and omitted requests continue to pass unchanged (AC7).
- AC2 Strict check: the merge uses `...(isApplicantsUpdated === true && { isApplicantsUpdated: true, applicantsUpdatedAt: new Date() })` at [src/modules/eoi_manager/voucher_forms/voucher_form.service.ts](src/modules/eoi_manager/voucher_forms/voucher_form.service.ts) lines 616–619, preserving the strict boolean contract; `false`, `undefined`, `"true"`, `1`, etc. all evaluate to no-op.
- AC3 Voucher update on success: when the flag is strictly `true`, the payload sets `isApplicantsUpdated: true` and `applicantsUpdatedAt: new Date()` — server-side timestamp, consistent with sibling `customerLastUpdatedAt: new Date()` in the same `updateData` object (line 615), which resolves spec Open Question 5.
- AC4 Atomicity: the implementation deviates from the plan's `manager.transaction(...)` wrapper and instead merges the applicant JSON payload and the flag fields into a single `voucherFormRepository.update({ voucherId }, updateData)` call (line 622). A single SQL `UPDATE` of one row is inherently atomic at the row/statement level — there is no path where the applicant JSON could be committed without the flag fields, or vice versa — so the spec's "applicant update and the voucher update must be committed in a single transaction" is satisfied by construction. The spec also requires "use the existing repository / transaction manager pattern already used inside `updateVoucherFormApplicant()` — do not introduce a new transaction strategy", and the existing pattern at this call site is exactly a single `repository.update`, which the change preserves. `refreshCacheToken(voucherForm.userVoucherTrackingId)` correctly runs only after the update resolves (line 623), so a rejected update skips cache invalidation.
- AC5 Voucher existence precondition: `getVoucherFormByVoucherId(voucherId)` at line 562 runs before the update; per its definition (lines 259–289) it queries with `voucher.voucherId = :voucherId AND voucher.isDeleted = false` and throws `NotFoundException('Voucher form not found')` when the row is absent or soft-deleted. This satisfies "verify the voucher record exists … fail the transaction with a clear, conventional error" without needing an `affected`-based guard on the subsequent `update` (the only window between the read and the write is a vanishingly small race, and a 0-row update would simply be a no-op against a row that no longer exists).
- AC6 Entity + migration: `isApplicantsUpdated` (boolean, default `false`) and `applicantsUpdatedAt` (timestamp, nullable, default `null`) added at [src/modules/eoi_manager/voucher_forms/entities/voucher_form.entity.ts](src/modules/eoi_manager/voucher_forms/entities/voucher_form.entity.ts) lines 326–339, placed next to `customerLastUpdatedAt`. Migration [src/migrations/1780000000000-AddIsApplicantsUpdatedToVouchers.ts](src/migrations/1780000000000-AddIsApplicantsUpdatedToVouchers.ts) executes raw `ALTER TABLE vouchers ADD COLUMN is_applicants_updated BOOLEAN NOT NULL DEFAULT FALSE` and `... applicants_updated_at TIMESTAMP NULL DEFAULT NULL`, both guarded by `queryRunner.hasColumn(...)` in `up()`; `down()` symmetrically guards before `DROP COLUMN`. The timestamp `1780000000000` follows the recent round-number convention (`1779800000000`, `1779900000000`).
- AC7 Backward compatibility: when the flag is omitted (or `false`), the conditional spread contributes nothing — `updateData` retains exactly the prior shape — and the response envelope `{ message: 'Applicant details updated successfully.', data: updateData }` is unchanged.
- AC8 Validation/conventions: validators are consistent with neighboring DTO fields; no endpoint URL/verb changes; the outer `try/catch → logger.error → throw error` envelope is unchanged so `success-response-errors` behavior is preserved.

JSON column hygiene: `isApplicantsUpdated` is destructured off `applicantDto` alongside `lastStep` and `applicantNumber` at lines 585–590 before `cleanedApplicantDto` is spread into the `applicantN` JSON blob, so the flag never lands inside the JSON applicant column — only into the dedicated top-level `vouchers` columns when strictly `=== true`. The service spec asserts this on every scenario (`expect(payload.applicant1).not.toHaveProperty('isApplicantsUpdated')`).

Test coverage:
- Controller spec ([src/modules/eoi_manager/voucher_forms/voucher_form.controller.spec.ts](src/modules/eoi_manager/voucher_forms/voucher_form.controller.spec.ts) lines 177–199) adds a focused pass-through assertion (`expect(passedDto.isApplicantsUpdated).toBe(true)`) without touching the pre-existing assertions.
- Service spec ([src/modules/eoi_manager/voucher_forms/voucher_form.service.spec.ts](src/modules/eoi_manager/voucher_forms/voucher_form.service.spec.ts)) covers the four scenarios that match the actual single-`update` implementation: (1) omitted flag → one `repository.update`, payload has no flag fields, JSON `applicant1` has no flag; (2) `false` → same as (1); (3) `true` → one `repository.update` merging `isApplicantsUpdated: true` + `applicantsUpdatedAt: instanceof Date`, JSON `applicant1` still clean; (4) `repository.update` rejection propagates and `refreshCacheToken` is not invoked. The constructor providers (`getRepositoryToken(VoucherForm/EoiCampaign/VoucherUnitMapping/VoucherUnitBlocking/Users)`, `CACHE_MANAGER`, `EventEmitter2`, `ConfigService`, `PdfService`, `AwsService`, `BookingsService`) line up with the service's actual injections.

Coverage observation (informational, not a defect): the spec's "Testing Notes" list five scenarios; the fifth one — "voucher not found while `isApplicantsUpdated === true` → transaction rolls back" — is not duplicated here because that path is already covered by `getVoucherFormByVoucherId` throwing `NotFoundException` upstream (its own behavior is tested independently and short-circuits before any voucher write). The current spec implicitly relies on that, which is consistent with the merged single-`update` design.

Plan vs. implementation note (informational): the implementation plan and the prior orchestrator artifacts described a `voucherFormRepository.manager.transaction(...)` wrapper with two (or one, post-collapse) `manager.update` calls and `affected`-based existence gating. The current implementation reverts to the simpler single `voucherFormRepository.update` pattern that was already in place at this call site. The spec's atomicity (AC4) and existence (AC5) guarantees are still met — atomicity by virtue of a single-row `UPDATE`, existence by the upstream `getVoucherFormByVoucherId` throw — so this is a plan deviation, not an AC violation. No code change required.

Extra changed files outside the planned target list — [docs/ai/stories/PE-596/spec.md](docs/ai/stories/PE-596/spec.md), [docs/ai/stories/PE-596/implementation-plan.md](docs/ai/stories/PE-596/implementation-plan.md), [src/migrations/1780000000000-AddIsApplicantsUpdatedToVouchers.ts](src/migrations/1780000000000-AddIsApplicantsUpdatedToVouchers.ts), [src/modules/eoi_manager/voucher_forms/voucher_form.service.spec.ts](src/modules/eoi_manager/voucher_forms/voucher_form.service.spec.ts), and `.opencode/executions/exec-1ac0f47f-a395-4c5e-bb82-ab59221b1774/{final-summary.md,review-pointers-cycle-1.md,working-tree.diff}` — are all expected story-analyzer / planner / orchestrator artifacts. The migration is explicitly in the planner's target list (`<timestamp>` placeholder resolved to `1780000000000`); the service spec was a "create recommended" entry in the plan; no accidental edits to unrelated modules detected.

Findings: None
