# Working Tree Diff
# Includes unstaged and staged changes.
# Generated by iProgrammer Flow for execution exec-e8af4162-61f6-4665-9360-c049c0bcad86.

## Unstaged Diff
diff --git a/src/modules/iom/constants.ts b/src/modules/iom/constants.ts
index 10807376..a05354ae 100644
--- a/src/modules/iom/constants.ts
+++ b/src/modules/iom/constants.ts
@@ -146,6 +146,15 @@ export const RATIO_SUM_TOLERANCE = 1e-6;
 
 export const IOM_HISTORY_EVENT = 'iom.history';
 
+/** Redis key prefix for LOYALTY tab count cache entries. */
+export const IOM_LOYALTY_COUNTS_CACHE_PREFIX = 'iom:counts:loyalty';
+
+/** Redis set index prefix mapping projectId -> cached count keys. */
+export const IOM_LOYALTY_COUNTS_CACHE_INDEX_PREFIX = 'iom:counts:loyalty:idx';
+
+/** TTL for LOYALTY tab count cache entries (10 minutes). */
+export const IOM_LOYALTY_COUNTS_CACHE_TTL_MS = 10 * 60 * 1000;
+
 /**
  * Stable machine-readable action labels written to `iom_history.action`.
  * Audit tooling, reporting, and analytics key off these strings - keep
diff --git a/src/modules/iom/iom.module.ts b/src/modules/iom/iom.module.ts
index 9b12eb0e..76f95f54 100644
--- a/src/modules/iom/iom.module.ts
+++ b/src/modules/iom/iom.module.ts
@@ -34,6 +34,8 @@ import { IomApproveService } from './services/iom-approve.service';
 import { IomCancelService } from './services/iom-cancel.service';
 import { IomSubmissionNotificationService } from './services/iom-submission-notification.service';
 import { IomExportService } from './services/iom-export.service';
+import { IomLoyaltyCountsCacheService } from './services/iom-loyalty-counts-cache.service';
+import { IomLoyaltyCountsCacheListener } from './services/iom-loyalty-counts-cache.listener';
 
 /**
  * CRM-USER-scoped IOM module.
@@ -79,6 +81,8 @@ import { IomExportService } from './services/iom-export.service';
     IomCancelService,
     IomSubmissionNotificationService,
     IomExportService,
+    IomLoyaltyCountsCacheService,
+    IomLoyaltyCountsCacheListener,
   ],
   exports: [
     WorkflowValidationService,
diff --git a/src/modules/iom/services/iom-export.service.spec.ts b/src/modules/iom/services/iom-export.service.spec.ts
index 380c14ca..0f5cbec4 100644
--- a/src/modules/iom/services/iom-export.service.spec.ts
+++ b/src/modules/iom/services/iom-export.service.spec.ts
@@ -122,7 +122,7 @@ describe('IomExportService', () => {
     expect(iomListingService.findIoms).toHaveBeenCalledWith(
       CRM_USER,
       {},
-      { skipPagination: true },
+      { skipPagination: true, skipCounts: true },
     );
     expect(generateExcelBuffer).toHaveBeenCalledWith(
       resolveExportColumns(undefined, RolesEnum.CRM),
@@ -157,7 +157,7 @@ describe('IomExportService', () => {
     expect(iomListingService.findIoms).toHaveBeenCalledWith(
       CRM_USER,
       fromExportIomExcelDto(dto),
-      { skipPagination: true },
+      { skipPagination: true, skipCounts: true },
     );
   });
 
diff --git a/src/modules/iom/services/iom-export.service.ts b/src/modules/iom/services/iom-export.service.ts
index 91dedc76..64748222 100644
--- a/src/modules/iom/services/iom-export.service.ts
+++ b/src/modules/iom/services/iom-export.service.ts
@@ -46,6 +46,7 @@ export class IomExportService {
       const filters = fromExportIomExcelDto(dto);
       const { items } = await this.iomListingService.findIoms(user, filters, {
         skipPagination: true,
+        skipCounts: true,
       });
       const rows = items.map((item) => this.toExportRow(item, columns));
       const buffer = await generateExcelBuffer(columns, rows, 'IOM Export');
diff --git a/src/modules/iom/services/iom-listing.service.spec.ts b/src/modules/iom/services/iom-listing.service.spec.ts
index 3c91347f..eb49267b 100644
--- a/src/modules/iom/services/iom-listing.service.spec.ts
+++ b/src/modules/iom/services/iom-listing.service.spec.ts
@@ -4,6 +4,7 @@ import { getRepositoryToken } from '@nestjs/typeorm';
 import { Repository, SelectQueryBuilder } from 'typeorm';
 
 import { IomListingService } from './iom-listing.service';
+import { IomLoyaltyCountsCacheService } from './iom-loyalty-counts-cache.service';
 import { Iom } from '../entities/iom.entity';
 import { IomStatusCodeEnum } from '../enums/iom-status-code.enum';
 import { ListIomListingDto } from '../dto/list-iom-listing.dto';
@@ -64,6 +65,9 @@ describe('IomListingService', () => {
   let listingQb: jest.Mocked<SelectQueryBuilder<Iom>>;
   let countQb: jest.Mocked<SelectQueryBuilder<Iom>>;
   let mappingRepo: jest.Mocked<Pick<Repository<ProjectUserMapping>, 'find'>>;
+  let loyaltyCountsCache: jest.Mocked<
+    Pick<IomLoyaltyCountsCacheService, 'getCounts'>
+  >;
   let qbCallIndex: number;
 
   const LOYALTY_USER: AuthenticatedUser = {
@@ -111,6 +115,10 @@ describe('IomListingService', () => {
       find: jest.fn().mockResolvedValue([makeMapping(10), makeMapping(11)]),
     } as unknown as jest.Mocked<Pick<Repository<ProjectUserMapping>, 'find'>>;
 
+    loyaltyCountsCache = {
+      getCounts: jest.fn(async (_scope, compute) => compute()),
+    };
+
     const module: TestingModule = await Test.createTestingModule({
       providers: [
         IomListingService,
@@ -119,6 +127,10 @@ describe('IomListingService', () => {
           provide: getRepositoryToken(ProjectUserMapping),
           useValue: mappingRepo,
         },
+        {
+          provide: IomLoyaltyCountsCacheService,
+          useValue: loyaltyCountsCache,
+        },
       ],
     }).compile();
 
@@ -249,11 +261,19 @@ describe('IomListingService', () => {
     );
   });
 
-  it('findAllForExport delegates to findIoms with skipPagination', async () => {
+  it('findAllForExport delegates to findIoms with skipPagination and skipCounts', async () => {
+    const findIomsSpy = jest.spyOn(service, 'findIoms');
+
     const items = await service.findAllForExport(CRM_USER, {
       search: 'jane',
     });
 
+    expect(findIomsSpy).toHaveBeenCalledWith(CRM_USER, { search: 'jane' }, {
+      skipPagination: true,
+      skipCounts: true,
+    });
+    findIomsSpy.mockRestore();
+
     expect(listingQb.skip).not.toHaveBeenCalled();
     expect(listingQb.take).not.toHaveBeenCalled();
     expect(listingQb.getMany).toHaveBeenCalled();
@@ -683,6 +703,31 @@ describe('IomListingService', () => {
 
       expect(result.counts).toBeUndefined();
       expect(countQb.getRawOne).not.toHaveBeenCalled();
+      expect(loyaltyCountsCache.getCounts).not.toHaveBeenCalled();
+    });
+
+    it('calls cache service for Loyalty listing counts', async () => {
+      await service.findIoms(LOYALTY_USER, {
+        listType: 'pendingSubmission',
+      } as unknown as ListIomListingDto);
+
+      expect(loyaltyCountsCache.getCounts).toHaveBeenCalledWith(
+        [10, 11],
+        expect.any(Function),
+      );
+    });
+
+    it('does not call cache service when skipCounts is true', async () => {
+      const result = await service.findIoms(
+        LOYALTY_USER,
+        {
+          listType: 'pendingSubmission',
+        } as unknown as ListIomListingDto,
+        { skipCounts: true },
+      );
+
+      expect(loyaltyCountsCache.getCounts).not.toHaveBeenCalled();
+      expect(result.counts).toBeUndefined();
     });
 
     it('ignores Loyalty listType tab filter for non-Loyalty roles', async () => {
diff --git a/src/modules/iom/services/iom-listing.service.ts b/src/modules/iom/services/iom-listing.service.ts
index dfef9ced..158f62f6 100644
--- a/src/modules/iom/services/iom-listing.service.ts
+++ b/src/modules/iom/services/iom-listing.service.ts
@@ -23,6 +23,7 @@ import {
   buildLoyaltyCountsSelect,
   isLoyaltyListType,
 } from '../utils/iom-loyalty-listing.util';
+import { IomLoyaltyCountsCacheService } from './iom-loyalty-counts-cache.service';
 
 const ALLOWED_SORT_FIELDS: Record<string, string> = {
   createdAt: 'i.createdAt',
@@ -34,6 +35,7 @@ const basePath = '';
 
 export interface FindIomsOptions {
   skipPagination?: boolean;
+  skipCounts?: boolean;
 }
 
 @Injectable()
@@ -43,6 +45,7 @@ export class IomListingService {
     private readonly iomRepo: Repository<Iom>,
     @InjectRepository(ProjectUserMapping)
     private readonly projectUserMappingRepo: Repository<ProjectUserMapping>,
+    private readonly loyaltyCountsCache: IomLoyaltyCountsCacheService,
   ) {}
 
   async findIoms(
@@ -51,6 +54,7 @@ export class IomListingService {
     options?: FindIomsOptions,
   ): Promise<IomListingResult> {
     const skipPagination = options?.skipPagination === true;
+    const skipCounts = options?.skipCounts === true;
 
     const isListingDto = this.isListIomListingDto(query);
     const page = isListingDto ? (query.page ?? DEFAULT_PAGE) : DEFAULT_PAGE;
@@ -109,8 +113,8 @@ export class IomListingService {
         page: 1,
         limit: rows.length,
         totalPages: rows.length > 0 ? 1 : 0,
-        ...(isLoyalty
-          ? { counts: await this.computeLoyaltyCounts(projectScope) }
+        ...(isLoyalty && !skipCounts
+          ? { counts: await this.resolveLoyaltyCounts(projectScope) }
           : {}),
       };
     }
@@ -123,8 +127,8 @@ export class IomListingService {
       page,
       limit,
       totalPages: Math.ceil(total / limit),
-      ...(isLoyalty
-        ? { counts: await this.computeLoyaltyCounts(projectScope) }
+      ...(isLoyalty && !skipCounts
+        ? { counts: await this.resolveLoyaltyCounts(projectScope) }
         : {}),
     };
   }
@@ -135,6 +139,7 @@ export class IomListingService {
   ): Promise<IomListItem[]> {
     const { items } = await this.findIoms(user, query, {
       skipPagination: true,
+      skipCounts: true,
     });
     return items;
   }
@@ -231,6 +236,14 @@ export class IomListingService {
     };
   }
 
+  private async resolveLoyaltyCounts(
+    projectScope: number[],
+  ): Promise<IomLoyaltyCounts> {
+    return this.loyaltyCountsCache.getCounts(projectScope, () =>
+      this.computeLoyaltyCounts(projectScope),
+    );
+  }
+
   private async computeLoyaltyCounts(
     projectScope: number[],
   ): Promise<IomLoyaltyCounts> {

## Staged Diff

(No staged index changes.)
