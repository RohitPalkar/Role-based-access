# Working Tree Diff
# Includes unstaged and staged changes.
# Generated by iProgrammer Flow for execution exec-ac585042-7d85-4582-bd95-98dd27603987.

## Unstaged Diff
diff --git a/.opencode/agents/unit-test-writer.md b/.opencode/agents/unit-test-writer.md
index e237b65a..ef538f70 100644
--- a/.opencode/agents/unit-test-writer.md
+++ b/.opencode/agents/unit-test-writer.md
@@ -37,6 +37,7 @@ On each run:
 
 This agent **writes** co-located `*.spec.ts` (and `test/*.e2e-spec.ts` only when plan requires). It does **not** modify production `src/` except spec files, `test/`, or `jest.config.js` unless explicitly instructed. It does **not** commit, push, or run migrations on shared DBs.
 
+This agent writes unit tests only for controller `*.spec.ts` files unless explicitly instructed otherwise.
 ---
 
 ## Workflow
diff --git a/src/config/constants.ts b/src/config/constants.ts
index 93f7a465..55383525 100644
--- a/src/config/constants.ts
+++ b/src/config/constants.ts
@@ -59,6 +59,7 @@ export const UNAUTHORIZE = HttpStatus.UNAUTHORIZED;
 export const RESET_VALIDATION_FAILED = HttpStatus.PARTIAL_CONTENT;
 export const MFA_SETUP_REQUIRED = HttpStatus.CONFLICT;
 export const SUCCESS = HttpStatus.OK;
+export const ACCEPTED = HttpStatus.ACCEPTED;
 export const TOO_MANY_REQUESTS = HttpStatus.TOO_MANY_REQUESTS;
 export const NOT_FOUND = HttpStatus.NOT_FOUND;
 export const FORBIDDEN = HttpStatus.FORBIDDEN;
@@ -201,3 +202,11 @@ export const RM_BUDDY_ALLOWED_ROLES = [
 ];
 
 export const BATCH_NOTIFICATION_QUEUE = 'batch-notifications';
+
+// PB-188 SFDC inbound webhook (`POST /api/sfdc/webhooks/lead-changes`)
+/** Env key for the SFDC webhook Basic Auth username (read via `CustomConfigService.getDecrypted`). */
+export const SFDC_WEBHOOK_BASIC_USER_KEY = 'SFDC_WEBHOOK_BASIC_USER';
+/** Env key for the SFDC webhook Basic Auth password (read via `CustomConfigService.getDecrypted`). */
+export const SFDC_WEBHOOK_BASIC_PASS_KEY = 'SFDC_WEBHOOK_BASIC_PASS';
+/** Notification `type` used for the "new SFDC change request awaiting review" admin notification. */
+export const SFDC_WEBHOOK_NOTIFICATION_TYPE = 'sfdc_voucher_change_request';
diff --git a/src/entities/index.ts b/src/entities/index.ts
index 6de9a1e8..37d66a9d 100644
--- a/src/entities/index.ts
+++ b/src/entities/index.ts
@@ -52,6 +52,7 @@ export { GroupBookingMapping } from 'src/modules/bookings/entities/group-booking
 export { Regions } from '../modules/masters/region/entities/region.entities';
 export { UserGroupAssignment } from '../modules/users/entities/user_group_assignment.entity';
 export { VoucherChangeRequest } from '../modules/eoi_manager/eoi_management/entities/source-change-request.entity';
+export { SfdcVoucherChangeRequest } from '../modules/sfdc/entities/sfdc-voucher-change-request.entity';
 
 export { VoucherUnitMapping } from 'src/modules/eoi_manager/voucher_forms/entities/voucher_unit_mappings.entity';
 export { ProjectInventoryUnit } from 'src/modules/inventory-unit/entities/project_inventory_units.entity';
diff --git a/src/modules/agreement_signature_form/agreement_signature_form.controller.spec.ts b/src/modules/agreement_signature_form/agreement_signature_form.controller.spec.ts
index 28407e61..191ddc47 100644
--- a/src/modules/agreement_signature_form/agreement_signature_form.controller.spec.ts
+++ b/src/modules/agreement_signature_form/agreement_signature_form.controller.spec.ts
@@ -1,6 +1,34 @@
-// Mock problematic modules before imports
+// Mock problematic modules before imports.
+// NOTE (PB-188): the more complete `debug` factory and the EventEmitter2 mock
+// below are pre-existing test-drift fixes pulled into this branch because the
+// previous `jest.mock('debug', () => jest.fn())` no longer satisfies
+// `ioredis`'s `genDebugFunction` after a transitive dependency bump, which
+// caused this spec to fail to compile when running the PB-188 SFDC webhook
+// specs alongside the existing suite. The change is unrelated to the SFDC
+// webhook itself and can be lifted into its own PR if preferred.
 jest.mock('puppeteer', () => ({}));
-jest.mock('debug', () => jest.fn());
+jest.mock('debug', () => {
+  const createDebugFn = () => {
+    const fn: any = (..._args: any[]) => undefined;
+    fn.enabled = false;
+    fn.namespace = '';
+    fn.color = 0;
+    fn.destroy = () => true;
+    fn.extend = () => createDebugFn();
+    fn.log = () => undefined;
+    return fn;
+  };
+  const factory: any = (_namespace?: string) => createDebugFn();
+  factory.default = factory;
+  factory.enable = () => undefined;
+  factory.disable = () => '';
+  factory.enabled = () => false;
+  factory.coerce = (v: any) => v;
+  factory.names = [];
+  factory.skips = [];
+  factory.formatters = {};
+  return factory;
+});
 jest.mock('src/modules/pdf/pdf.service', () => ({
   PdfService: jest.fn().mockImplementation(() => ({
     generatePdf: jest.fn(),
@@ -16,6 +44,7 @@ import { Test, TestingModule } from '@nestjs/testing';
 import { AgreementSignatureFormController } from './agreement_signature_form.controller';
 import { AgreementSignatureFormService } from './agreement_signature_form.service';
 import { BadRequestException, NotFoundException } from '@nestjs/common';
+import { EventEmitter2 } from '@nestjs/event-emitter';
 
 describe('AgreementSignatureFormController', () => {
   let controller: AgreementSignatureFormController;
@@ -44,6 +73,13 @@ describe('AgreementSignatureFormController', () => {
           provide: AgreementSignatureFormService,
           useValue: mockService,
         },
+        {
+          provide: EventEmitter2,
+          useValue: {
+            emit: jest.fn(),
+            emitAsync: jest.fn(),
+          },
+        },
       ],
     }).compile();
 
diff --git a/src/modules/eoi_manager/eoi_campaign/eoi_campaign.controller.spec.ts b/src/modules/eoi_manager/eoi_campaign/eoi_campaign.controller.spec.ts
index 221194bb..2afd329e 100644
--- a/src/modules/eoi_manager/eoi_campaign/eoi_campaign.controller.spec.ts
+++ b/src/modules/eoi_manager/eoi_campaign/eoi_campaign.controller.spec.ts
@@ -1,3 +1,11 @@
+// NOTE (PB-188): three mock DTOs in this spec gained `venueName: 'Sample Venue'`
+// and `agreementDocLink: 'https://example.com/agreement.pdf'` to match an
+// upstream `CreateEoiCampaignDto` field addition that landed before this
+// branch was cut. The SFDC webhook work in PB-188 does not touch
+// `EoiCampaignController` itself; this edit is purely test-drift maintenance
+// surfaced when the test runner was widened to include this spec (see
+// review-pointers-cycle-3.md R4). If preferred at review time, this two-key
+// addition can be lifted into its own PR.
 import { Test, TestingModule } from '@nestjs/testing';
 import { EoiCampaignController } from './eoi_campaign.controller';
 import { EoiCampaignService } from './eoi_campaign.service';
@@ -55,6 +63,8 @@ describe('EoiCampaignController', () => {
     enquiryCounter: 1,
     displayQueueId: DisplayQueueIdEnum.ALL,
     availableGateways: [PaymentGatewayEnum.EASEBUZZ],
+    venueName: 'Sample Venue',
+    agreementDocLink: 'https://example.com/agreement.pdf',
   };
 
   beforeEach(async () => {
@@ -378,6 +388,8 @@ describe('EoiCampaignController', () => {
         enquiryCounter: 1,
         displayQueueId: DisplayQueueIdEnum.ALL,
         availableGateways: [PaymentGatewayEnum.EASEBUZZ],
+        venueName: 'Sample Venue',
+        agreementDocLink: 'https://example.com/agreement.pdf',
       };
 
       const mockResponse = {
@@ -1218,6 +1230,8 @@ describe('EoiCampaignController', () => {
       enquiryCounter: 1,
       displayQueueId: DisplayQueueIdEnum.ALL,
       availableGateways: [PaymentGatewayEnum.EASEBUZZ],
+      venueName: 'Sample Venue',
+      agreementDocLink: 'https://example.com/agreement.pdf',
     };
 
     it('should update EOI campaign successfully (success + response time)', async () => {
diff --git a/src/modules/eoi_manager/eoi_management/eoi_management.controller.spec.ts b/src/modules/eoi_manager/eoi_management/eoi_management.controller.spec.ts
index 5adaccc4..825cffdb 100644
--- a/src/modules/eoi_manager/eoi_management/eoi_management.controller.spec.ts
+++ b/src/modules/eoi_manager/eoi_management/eoi_management.controller.spec.ts
@@ -1,3 +1,10 @@
+// NOTE (PB-188): two mock DTOs in this spec gained `residentStatus: 'INDIAN'`
+// to match an upstream `CreateVoucherFormDto` field addition that landed
+// before this branch was cut. The SFDC webhook work in PB-188 does not
+// touch `EoiManagementController` itself; this edit is purely test-drift
+// maintenance surfaced when the test runner was widened to include this
+// spec (see review-pointers-cycle-3.md R4). If preferred at review time,
+// this one-key addition can be lifted into its own PR.
 import { Test, TestingModule } from '@nestjs/testing';
 import { EoiManagementController } from './eoi_management.controller';
 import { EoiManagementService } from './eoi_management.service';
@@ -101,6 +108,7 @@ describe('EoiManagementController', () => {
     emailId: 'john@example.com',
     campaignId: 1,
     primarySource: PrimarySourceEnum.DIRECT_WALKIN,
+    residentStatus: 'INDIAN',
   };
 
   const mockUpdateVoucherDetailsDto: UpdateVoucherDetailsDto = {
@@ -127,6 +135,7 @@ describe('EoiManagementController', () => {
       ],
       lastStep: 3,
     } as any,
+    residentStatus: 'INDIAN',
   };
 
   const mockUser = {
diff --git a/src/modules/sfdc/sfdc.module.ts b/src/modules/sfdc/sfdc.module.ts
index 76e34567..6ddfa15f 100644
--- a/src/modules/sfdc/sfdc.module.ts
+++ b/src/modules/sfdc/sfdc.module.ts
@@ -1,12 +1,22 @@
 import { Module } from '@nestjs/common';
-import { SfdcService } from './sfdc.service';
 import { HttpModule } from '@nestjs/axios';
+import { TypeOrmModule } from '@nestjs/typeorm';
+
+import { SfdcService } from './sfdc.service';
 import { SfdcController } from './sfdc.controller';
+import { SfdcWebhookController } from './sfdc-webhook.controller';
+import { SfdcWebhookService } from './sfdc-webhook.service';
+import { SfdcWebhookBasicAuthGuard } from './guards/sfdc-webhook-basic-auth.guard';
+import { SfdcVoucherChangeRequest } from './entities/sfdc-voucher-change-request.entity';
+import { VoucherForm } from '../eoi_manager/voucher_forms/entities/voucher_form.entity';
 
 @Module({
-  imports: [HttpModule],
-  providers: [SfdcService],
-  controllers: [SfdcController],
+  imports: [
+    HttpModule,
+    TypeOrmModule.forFeature([VoucherForm, SfdcVoucherChangeRequest]),
+  ],
+  providers: [SfdcService, SfdcWebhookService, SfdcWebhookBasicAuthGuard],
+  controllers: [SfdcController, SfdcWebhookController],
   exports: [SfdcService],
 })
 export class SfdcModule {}

## Staged Diff

(No staged index changes.)
