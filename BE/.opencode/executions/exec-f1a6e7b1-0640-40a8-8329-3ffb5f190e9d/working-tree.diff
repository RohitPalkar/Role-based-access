# Working Tree Diff
# Includes unstaged and staged changes.
# Generated by iProgrammer Flow for execution exec-f1a6e7b1-0640-40a8-8329-3ffb5f190e9d.

## Unstaged Diff
diff --git a/src/modules/users/services/user-availability.service.spec.ts b/src/modules/users/services/user-availability.service.spec.ts
index e0b18972..261c4a27 100644
--- a/src/modules/users/services/user-availability.service.spec.ts
+++ b/src/modules/users/services/user-availability.service.spec.ts
@@ -3,6 +3,7 @@ import {
   BadRequestException,
   ConflictException,
   ForbiddenException,
+  InternalServerErrorException,
 } from '@nestjs/common';
 import { getRepositoryToken } from '@nestjs/typeorm';
 
@@ -12,6 +13,22 @@ import { UserAvailability } from '../entities/user-availability.entity';
 import { UserAvailabilityService } from './user-availability.service';
 import { UserService } from '../user.service';
 import { Users } from '../entities/user.entity';
+import { AwsService } from 'src/modules/aws/aws.service';
+import { CustomConfigService } from 'src/config/custom-config.service';
+import { generateExcelBuffer } from 'src/common/helpers/excel.helper';
+import { uploadBufferToS3 } from 'src/common/helpers/s3-upload.helper';
+
+jest.mock('src/common/helpers/excel.helper', () => ({
+  generateExcelBuffer: jest.fn(),
+}));
+
+jest.mock('src/common/helpers/s3-upload.helper', () => ({
+  uploadBufferToS3: jest.fn(),
+}));
+
+jest.mock('src/helpers/date.helper', () => ({
+  formatDateUtil: jest.fn().mockReturnValue('17Jun_3-30PM'),
+}));
 
 describe('UserAvailabilityService', () => {
   let service: UserAvailabilityService;
@@ -36,8 +53,11 @@ describe('UserAvailabilityService', () => {
     innerJoin: jest.Mock;
     where: jest.Mock;
     andWhere: jest.Mock;
+    setParameter: jest.Mock;
+    skip: jest.Mock;
+    take: jest.Mock;
     select: jest.Mock;
-    getMany: jest.Mock;
+    getManyAndCount: jest.Mock;
   };
   let iomCountQb: {
     select: jest.Mock;
@@ -56,6 +76,7 @@ describe('UserAvailabilityService', () => {
 
   const tlUser = { dbId: 10 };
   const targetUserId = 20;
+  const listQuery = { status: 'ALL' } as any;
 
   const futureFrom = () => new Date(Date.now() + 60 * 60 * 1000).toISOString();
   const futureTo = () =>
@@ -90,8 +111,11 @@ describe('UserAvailabilityService', () => {
       innerJoin: jest.fn().mockReturnThis(),
       where: jest.fn().mockReturnThis(),
       andWhere: jest.fn().mockReturnThis(),
+      setParameter: jest.fn().mockReturnThis(),
+      skip: jest.fn().mockReturnThis(),
+      take: jest.fn().mockReturnThis(),
       select: jest.fn().mockReturnThis(),
-      getMany: jest.fn().mockResolvedValue([]),
+      getManyAndCount: jest.fn().mockResolvedValue([[], 0]),
     };
 
     iomCountQb = {
@@ -155,6 +179,14 @@ describe('UserAvailabilityService', () => {
           provide: getRepositoryToken(ProjectUserMapping),
           useValue: projectUserMappingRepo,
         },
+        {
+          provide: AwsService,
+          useValue: { uploadToS3: jest.fn() },
+        },
+        {
+          provide: CustomConfigService,
+          useValue: { get: jest.fn() },
+        },
       ],
     }).compile();
 
@@ -529,41 +561,48 @@ describe('UserAvailabilityService', () => {
       empCode: 'CRM1021',
     };
 
-    it('returns an empty array when the TL has no direct-report CRM users', async () => {
-      teamMembersQb.getMany.mockResolvedValueOnce([]);
+    it('returns an empty paginated result when the TL has no direct-report CRM users', async () => {
+      teamMembersQb.getManyAndCount.mockResolvedValueOnce([[], 0]);
 
-      const result = await service.getTeamAvailability(tlUser);
+      const result = await service.getTeamAvailability(tlUser, listQuery);
 
-      expect(result).toEqual([]);
+      expect(result).toEqual({
+        items: [],
+        page: 1,
+        limit: 10,
+        total: 0,
+        totalPages: 0,
+      });
       expect(availabilityRepo.createQueryBuilder).not.toHaveBeenCalled();
     });
 
     it('returns team members with AVAILABLE status when no active window exists', async () => {
-      teamMembersQb.getMany.mockResolvedValueOnce([teamMember]);
+      teamMembersQb.getManyAndCount.mockResolvedValueOnce([[teamMember], 1]);
       availabilityQb.getMany.mockResolvedValueOnce([]);
 
-      const result = await service.getTeamAvailability(tlUser);
+      const result = await service.getTeamAvailability(tlUser, listQuery);
 
-      expect(result).toHaveLength(1);
-      expect(result[0]).toEqual({
+      expect(result.items).toHaveLength(1);
+      expect(result.items[0]).toEqual({
         userId: 101,
         empId: 'CRM1021',
         name: 'Amit Sharma',
         email: 'amit@company.com',
         role: RolesEnum.CRM,
         currentStatus: 'AVAILABLE',
+        statusLabel: 'Available',
         projects: [],
         allocatedIomsCount: 0,
       });
-      expect(result[0]).not.toHaveProperty('unavailableFrom');
-      expect(result[0]).not.toHaveProperty('unavailableTo');
+      expect(result.items[0]).not.toHaveProperty('unavailableFrom');
+      expect(result.items[0]).not.toHaveProperty('unavailableTo');
     });
 
     it('returns UNAVAILABLE status with window fields for active users', async () => {
       const unavailableFrom = new Date('2026-06-15T11:00:00.000Z');
       const unavailableTo = new Date('2026-06-15T16:00:00.000Z');
 
-      teamMembersQb.getMany.mockResolvedValueOnce([teamMember]);
+      teamMembersQb.getManyAndCount.mockResolvedValueOnce([[teamMember], 1]);
       availabilityQb.getMany.mockResolvedValueOnce([
         {
           userId: 101,
@@ -572,15 +611,17 @@ describe('UserAvailabilityService', () => {
         },
       ]);
 
-      const result = await service.getTeamAvailability(tlUser);
+      const result = await service.getTeamAvailability(tlUser, listQuery);
 
-      expect(result[0].currentStatus).toBe('UNAVAILABLE');
-      expect(result[0].unavailableFrom).toBe(unavailableFrom.toISOString());
-      expect(result[0].unavailableTo).toBe(unavailableTo.toISOString());
+      expect(result.items[0].currentStatus).toBe('UNAVAILABLE');
+      expect(result.items[0].unavailableFrom).toBe(
+        unavailableFrom.toISOString(),
+      );
+      expect(result.items[0].unavailableTo).toBe(unavailableTo.toISOString());
     });
 
     it('includes allocated IOM counts and mapped projects', async () => {
-      teamMembersQb.getMany.mockResolvedValueOnce([teamMember]);
+      teamMembersQb.getManyAndCount.mockResolvedValueOnce([[teamMember], 1]);
       availabilityQb.getMany.mockResolvedValueOnce([]);
       iomCountQb.getRawMany.mockResolvedValueOnce([
         { userId: 101, count: '12' },
@@ -592,10 +633,12 @@ describe('UserAvailabilityService', () => {
         },
       ]);
 
-      const result = await service.getTeamAvailability(tlUser);
+      const result = await service.getTeamAvailability(tlUser, listQuery);
 
-      expect(result[0].allocatedIomsCount).toBe(12);
-      expect(result[0].projects).toEqual([{ id: 58, name: 'Project Alpha' }]);
+      expect(result.items[0].allocatedIomsCount).toBe(12);
+      expect(result.items[0].projects).toEqual([
+        { id: 58, name: 'Project Alpha' },
+      ]);
     });
 
     it('uses the latest active window when multiple overlap', async () => {
@@ -603,7 +646,7 @@ describe('UserAvailabilityService', () => {
       const laterFrom = new Date('2026-06-15T11:00:00.000Z');
       const unavailableTo = new Date('2026-06-15T16:00:00.000Z');
 
-      teamMembersQb.getMany.mockResolvedValueOnce([teamMember]);
+      teamMembersQb.getManyAndCount.mockResolvedValueOnce([[teamMember], 1]);
       availabilityQb.getMany.mockResolvedValueOnce([
         {
           userId: 101,
@@ -617,9 +660,127 @@ describe('UserAvailabilityService', () => {
         },
       ]);
 
-      const result = await service.getTeamAvailability(tlUser);
+      const result = await service.getTeamAvailability(tlUser, listQuery);
+
+      expect(result.items[0].unavailableFrom).toBe(laterFrom.toISOString());
+    });
+
+    it('skips pagination when skipPagination is true', async () => {
+      teamMembersQb.getManyAndCount.mockResolvedValueOnce([[teamMember], 1]);
+      availabilityQb.getMany.mockResolvedValueOnce([]);
+
+      const result = await service.getTeamAvailability(tlUser, listQuery, {
+        skipPagination: true,
+      });
+
+      expect(teamMembersQb.skip).not.toHaveBeenCalled();
+      expect(teamMembersQb.take).not.toHaveBeenCalled();
+      expect(result.page).toBe(1);
+      expect(result.limit).toBe(1);
+      expect(result.totalPages).toBe(1);
+    });
+  });
+
+  describe('exportTeamAvailability', () => {
+    const teamMember = {
+      id: 101,
+      name: 'Amit Sharma',
+      email: 'amit@company.com',
+      empCode: 'CRM1021',
+    };
+
+    beforeEach(() => {
+      (generateExcelBuffer as jest.Mock).mockResolvedValue(
+        Buffer.from('excel'),
+      );
+      (uploadBufferToS3 as jest.Mock).mockResolvedValue(
+        'https://cdn.example.com/puravankara/exports/team-availability-17Jun_3-30PM.xlsx',
+      );
+    });
+
+    it('exports all filtered rows and returns the S3 URL', async () => {
+      teamMembersQb.getManyAndCount.mockResolvedValueOnce([[teamMember], 1]);
+      availabilityQb.getMany.mockResolvedValueOnce([]);
+      projectMappingQb.getMany.mockResolvedValueOnce([
+        {
+          user: { id: 101 },
+          project: { id: 58, name: 'Project Alpha' },
+        },
+        {
+          user: { id: 101 },
+          project: { id: 59, name: 'Project Beta' },
+        },
+      ]);
+
+      const result = await service.exportTeamAvailability(tlUser, listQuery);
+
+      expect(teamMembersQb.skip).not.toHaveBeenCalled();
+      expect(teamMembersQb.take).not.toHaveBeenCalled();
+      expect(generateExcelBuffer).toHaveBeenCalledWith(
+        expect.arrayContaining([
+          expect.objectContaining({ header: 'Employee ID', key: 'employeeId' }),
+        ]),
+        [
+          expect.objectContaining({
+            employeeId: 'CRM1021',
+            employeeName: 'Amit Sharma',
+            email: 'amit@company.com',
+            project: 'Project Alpha, Project Beta',
+            iomAllotted: 0,
+            statusLabel: 'Available',
+            fromDateTime: '',
+            toDateTime: '',
+          }),
+        ],
+      );
+      expect(uploadBufferToS3).toHaveBeenCalledWith(
+        expect.anything(),
+        expect.anything(),
+        expect.any(Buffer),
+        'exports/team-availability-17Jun_3-30PM.xlsx',
+        'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
+      );
+      expect(result).toEqual({
+        message: 'Team availability exported successfully',
+        data: {
+          url: 'https://cdn.example.com/puravankara/exports/team-availability-17Jun_3-30PM.xlsx',
+        },
+      });
+    });
+
+    it('maps unavailable date fields for UNAVAILABLE users', async () => {
+      const unavailableFrom = new Date('2026-06-15T11:00:00.000Z');
+      const unavailableTo = new Date('2026-06-15T16:00:00.000Z');
+
+      teamMembersQb.getManyAndCount.mockResolvedValueOnce([[teamMember], 1]);
+      availabilityQb.getMany.mockResolvedValueOnce([
+        {
+          userId: 101,
+          unavailableFrom,
+          unavailableTo,
+        },
+      ]);
+
+      await service.exportTeamAvailability(tlUser, listQuery);
 
-      expect(result[0].unavailableFrom).toBe(laterFrom.toISOString());
+      expect(generateExcelBuffer).toHaveBeenCalledWith(expect.any(Array), [
+        expect.objectContaining({
+          fromDateTime: unavailableFrom.toISOString(),
+          toDateTime: unavailableTo.toISOString(),
+        }),
+      ]);
+    });
+
+    it('throws InternalServerErrorException when export fails', async () => {
+      (generateExcelBuffer as jest.Mock).mockRejectedValueOnce(
+        new Error('excel failed'),
+      );
+      teamMembersQb.getManyAndCount.mockResolvedValueOnce([[teamMember], 1]);
+      availabilityQb.getMany.mockResolvedValueOnce([]);
+
+      await expect(
+        service.exportTeamAvailability(tlUser, listQuery),
+      ).rejects.toThrow(InternalServerErrorException);
     });
   });
 });
diff --git a/src/modules/users/services/user-availability.service.ts b/src/modules/users/services/user-availability.service.ts
index c7e8491e..d7e77082 100644
--- a/src/modules/users/services/user-availability.service.ts
+++ b/src/modules/users/services/user-availability.service.ts
@@ -2,6 +2,7 @@ import {
   BadRequestException,
   ConflictException,
   Injectable,
+  InternalServerErrorException,
 } from '@nestjs/common';
 import { InjectRepository } from '@nestjs/typeorm';
 import { Repository } from 'typeorm';
@@ -20,6 +21,26 @@ import { Users } from '../entities/user.entity';
 import { Iom, ProjectUserMapping } from 'src/entities';
 import { RolesEnum } from 'src/enums/roles.enum';
 import { toTitleCase } from 'src/helpers';
+import { formatDateUtil } from 'src/helpers/date.helper';
+import { generateExcelBuffer } from 'src/common/helpers/excel.helper';
+import { uploadBufferToS3 } from 'src/common/helpers/s3-upload.helper';
+import { AwsService } from 'src/modules/aws/aws.service';
+import { CustomConfigService } from 'src/config/custom-config.service';
+import { logger } from 'src/logger/logger';
+
+const TEAM_AVAILABILITY_EXCEL_MIME =
+  'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet';
+
+const TEAM_AVAILABILITY_EXPORT_COLUMNS = [
+  { header: 'Employee ID', key: 'employeeId', width: 15 },
+  { header: 'Employee Name', key: 'employeeName', width: 25 },
+  { header: 'Email ID', key: 'email', width: 30 },
+  { header: 'Project', key: 'project', width: 30 },
+  { header: 'IOM Allotted', key: 'iomAllotted', width: 15 },
+  { header: 'Status', key: 'statusLabel', width: 15 },
+  { header: 'From Date & Time', key: 'fromDateTime', width: 25 },
+  { header: 'To Date & Time', key: 'toDateTime', width: 25 },
+];
 
 @Injectable()
 export class UserAvailabilityService {
@@ -33,6 +54,8 @@ export class UserAvailabilityService {
     @InjectRepository(ProjectUserMapping)
     private readonly projectUserMappingRepo: Repository<ProjectUserMapping>,
     private readonly userService: UserService,
+    private readonly awsService: AwsService,
+    private readonly configService: CustomConfigService,
   ) {}
 
   async markUnavailable(
@@ -119,6 +142,7 @@ export class UserAvailabilityService {
   async getTeamAvailability(
     loggedInUser: { dbId: number },
     query: ListTeamAvailabilityDto,
+    options?: { skipPagination?: boolean },
   ): Promise<{
     items: TeamMemberAvailabilityDto[];
     page: number;
@@ -191,17 +215,27 @@ export class UserAvailabilityService {
     }
 
     /* PAGINATION */
-    qb.skip(skip).take(limit);
+    if (!options?.skipPagination) {
+      qb.skip(skip).take(limit);
+    }
 
     qb.select(['u.id', 'u.name', 'u.email', 'u.empCode']);
 
     const [teamMembers, total] = await qb.getManyAndCount();
 
+    const responsePage = options?.skipPagination ? 1 : page;
+    const responseLimit = options?.skipPagination ? total : limit;
+    const responseTotalPages = options?.skipPagination
+      ? total > 0
+        ? 1
+        : 0
+      : Math.ceil(total / limit);
+
     if (teamMembers.length === 0) {
       return {
         items: [],
-        page,
-        limit,
+        page: responsePage,
+        limit: responseLimit,
         total,
         totalPages: 0,
       };
@@ -243,13 +277,66 @@ export class UserAvailabilityService {
 
     return {
       items,
-      page,
-      limit,
+      page: responsePage,
+      limit: responseLimit,
       total,
-      totalPages: Math.ceil(total / limit),
+      totalPages: responseTotalPages,
     };
   }
 
+  async exportTeamAvailability(
+    loggedInUser: { dbId: number },
+    query: ListTeamAvailabilityDto,
+  ): Promise<{ message: string; data: { url: string } }> {
+    try {
+      const { items } = await this.getTeamAvailability(loggedInUser, query, {
+        skipPagination: true,
+      });
+
+      const rows = items.map((item) => ({
+        employeeId: item.empId ?? '',
+        employeeName: item.name ?? '',
+        email: item.email ?? '',
+        project: (item.projects ?? [])
+          .map((project) => project.name)
+          .join(', '),
+        iomAllotted: item.allocatedIomsCount ?? 0,
+        statusLabel: item.statusLabel ?? '',
+        fromDateTime:
+          item.currentStatus === 'AVAILABLE'
+            ? ''
+            : (item.unavailableFrom ?? ''),
+        toDateTime:
+          item.currentStatus === 'AVAILABLE' ? '' : (item.unavailableTo ?? ''),
+      }));
+
+      const buffer = await generateExcelBuffer(
+        TEAM_AVAILABILITY_EXPORT_COLUMNS,
+        rows,
+      );
+
+      const timestamp = formatDateUtil(undefined, 'timestamp');
+      const fileName = `exports/team-availability-${timestamp}.xlsx`;
+      const url = await uploadBufferToS3(
+        this.awsService,
+        this.configService,
+        buffer,
+        fileName,
+        TEAM_AVAILABILITY_EXCEL_MIME,
+      );
+
+      return {
+        message: 'Team availability exported successfully',
+        data: { url },
+      };
+    } catch (error) {
+      logger.error('Team availability export failed:', error);
+      throw new InternalServerErrorException(
+        'Failed to export team availability',
+      );
+    }
+  }
+
   private async resolveRelevantWindow(
     userId: number,
     now: Date,
diff --git a/src/modules/users/user.controller.ts b/src/modules/users/user.controller.ts
index bfd59ab2..a9fc07f1 100644
--- a/src/modules/users/user.controller.ts
+++ b/src/modules/users/user.controller.ts
@@ -313,4 +313,14 @@ export class UserController {
     );
     return { data: result };
   }
+
+  @Get('team/availability/export')
+  @UseGuards(RmAdminAuthGuard, RolesGuard)
+  @Roles(RolesEnum.CRM_TL)
+  async exportTeamAvailability(
+    @User() user: { dbId: number },
+    @Query() query: ListTeamAvailabilityDto,
+  ) {
+    return this.userAvailabilityService.exportTeamAvailability(user, query);
+  }
 }

## Staged Diff

(No staged index changes.)
