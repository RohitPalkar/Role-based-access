# Working Tree Diff
# Includes unstaged and staged changes.
# Generated by iProgrammer Flow for execution exec-f02320c0-05ba-404a-97fa-681a8eb4d08b.

## Unstaged Diff
diff --git a/src/constants/iom-export.columns.spec.ts b/src/constants/iom-export.columns.spec.ts
index 7fd94a86..5fdf186c 100644
--- a/src/constants/iom-export.columns.spec.ts
+++ b/src/constants/iom-export.columns.spec.ts
@@ -1,43 +1,116 @@
 import { BadRequestException } from '@nestjs/common';
 
-import { resolveExportColumns } from './iom-export.columns';
+import { RolesEnum } from 'src/enums/roles.enum';
+import {
+  getRoleAllowedExportColumnKeys,
+  IOM_EXPORT_BASE_COLUMN_KEYS,
+  resolveExportColumns,
+} from './iom-export.columns';
+
+describe('getRoleAllowedExportColumnKeys', () => {
+  it('returns base columns for unlisted roles', () => {
+    expect(getRoleAllowedExportColumnKeys(RolesEnum.CRM)).toEqual([
+      ...IOM_EXPORT_BASE_COLUMN_KEYS,
+    ]);
+  });
+
+  it('adds crmCreatedByName for CRM TL', () => {
+    const keys = getRoleAllowedExportColumnKeys(RolesEnum.CRM_TL);
+    expect(keys).toContain('crmCreatedByName');
+    expect(keys).not.toContain('crmVerifiedByName');
+  });
+
+  it('adds crmCreatedByName and crmVerifiedByName for CRM Head', () => {
+    const keys = getRoleAllowedExportColumnKeys(RolesEnum.CRM_HEAD);
+    expect(keys).toEqual([
+      ...IOM_EXPORT_BASE_COLUMN_KEYS,
+      'crmCreatedByName',
+      'crmVerifiedByName',
+    ]);
+  });
+
+  it('adds finance approval columns for Loyalty', () => {
+    const keys = getRoleAllowedExportColumnKeys(RolesEnum.LOYALTY);
+    expect(keys).toContain('financeApprovedByName');
+    expect(keys).toContain('financeVerifiedByName');
+    expect(keys).toContain('crmApprovedByName');
+  });
+});
 
 describe('resolveExportColumns', () => {
-  it('excludes statusCode and crmVerifiedBy from default export', () => {
-    const columns = resolveExportColumns();
+  it('returns role-based default columns for CRM', () => {
+    const columns = resolveExportColumns(undefined, RolesEnum.CRM);
     const keys = columns.map((column) => column.key);
 
+    expect(keys).toEqual([...IOM_EXPORT_BASE_COLUMN_KEYS]);
+    expect(keys).not.toContain('crmCreatedByName');
     expect(keys).not.toContain('statusCode');
-    expect(keys).not.toContain('crmVerifiedBy');
-    expect(keys).toContain('statusLabel');
-    expect(keys).toContain('crmVerifiedByName');
   });
 
-  it('excludes statusCode and crmVerifiedBy when fields is empty', () => {
-    const columns = resolveExportColumns([]);
+  it('returns role-based default columns when fields is empty', () => {
+    const columns = resolveExportColumns([], RolesEnum.CRM_TL);
     const keys = columns.map((column) => column.key);
 
-    expect(keys).not.toContain('statusCode');
-    expect(keys).not.toContain('crmVerifiedBy');
+    expect(keys).toEqual([...IOM_EXPORT_BASE_COLUMN_KEYS, 'crmCreatedByName']);
+  });
+
+  it('includes CRM TL role columns by default', () => {
+    const columns = resolveExportColumns(undefined, RolesEnum.CRM_TL);
+    const keys = columns.map((column) => column.key);
+
+    expect(keys).toContain('crmCreatedByName');
+    expect(keys).not.toContain('crmVerifiedByName');
   });
 
-  it('returns only requested columns when fields are provided', () => {
-    const columns = resolveExportColumns(['iomNo', 'statusLabel']);
+  it('excludes financeVerifiedByName for Finance User by default', () => {
+    const columns = resolveExportColumns(undefined, RolesEnum.FINANCE_USER);
     const keys = columns.map((column) => column.key);
 
-    expect(keys).toEqual(['iomNo', 'statusLabel']);
+    expect(keys).toContain('crmApprovedByName');
+    expect(keys).not.toContain('financeVerifiedByName');
   });
 
-  it('allows statusCode when explicitly requested', () => {
-    const columns = resolveExportColumns(['statusCode']);
+  it('returns only requested columns that pass role gate', () => {
+    const columns = resolveExportColumns(
+      ['iomNo', 'statusLabel', 'crmCreatedByName'],
+      RolesEnum.CRM_TL,
+    );
+    const keys = columns.map((column) => column.key);
+
+    expect(keys).toEqual(['iomNo', 'statusLabel', 'crmCreatedByName']);
+  });
+
+  it('silently drops role-disallowed fields', () => {
+    const columns = resolveExportColumns(
+      ['financeApprovedByName', 'iomNo'],
+      RolesEnum.CRM_TL,
+    );
+    const keys = columns.map((column) => column.key);
+
+    expect(keys).toEqual(['iomNo']);
+  });
+
+  it('drops statusCode when explicitly requested but not allowed for role', () => {
+    const columns = resolveExportColumns(['statusCode'], RolesEnum.CRM);
     const keys = columns.map((column) => column.key);
 
-    expect(keys).toEqual(['statusCode']);
+    expect(keys).toEqual([]);
   });
 
   it('throws BadRequestException for unknown fields', () => {
-    expect(() => resolveExportColumns(['notAField'])).toThrow(
+    expect(() => resolveExportColumns(['notAField'], RolesEnum.CRM)).toThrow(
       BadRequestException,
     );
   });
+
+  it('preserves deterministic ordering for default CRM Head export', () => {
+    const columns = resolveExportColumns(undefined, RolesEnum.CRM_HEAD);
+    const keys = columns.map((column) => column.key);
+
+    expect(keys).toEqual([
+      ...IOM_EXPORT_BASE_COLUMN_KEYS,
+      'crmCreatedByName',
+      'crmVerifiedByName',
+    ]);
+  });
 });
diff --git a/src/constants/iom-export.columns.ts b/src/constants/iom-export.columns.ts
index 2f151d72..8a00191b 100644
--- a/src/constants/iom-export.columns.ts
+++ b/src/constants/iom-export.columns.ts
@@ -1,15 +1,59 @@
 import { BadRequestException } from '@nestjs/common';
 
+import { RolesEnum } from 'src/enums/roles.enum';
+
 export interface IomExportColumn {
   header: string;
   key: string;
   width?: number;
 }
 
-export const DEFAULT_EXPORT_EXCLUDED_KEYS = new Set([
-  'statusCode',
-  'crmVerifiedBy',
-]);
+export const IOM_EXPORT_BASE_COLUMN_KEYS = [
+  'projectName',
+  'unitNo',
+  'customerName',
+  'saleValue',
+  'saleValueCollectedPercentage',
+  'statusLabel',
+  'iomCreatedAt',
+  'iomNo',
+  'thresholdPaymentReceivedAt',
+  'totalBrokerageAmount',
+  'referralPointsAdjustment',
+  'referralPointsEdited',
+  'loyaltyPointClassification',
+  'pointsUpdatedAt',
+  'invoiceReqNumber',
+  'invoiceStatus',
+  'invoiceNumber',
+  'invoiceDate',
+  'saleValueAmountCollected',
+] as const;
+
+const IOM_EXPORT_ROLE_COLUMN_KEYS: Partial<
+  Record<RolesEnum, readonly string[]>
+> = {
+  [RolesEnum.CRM_TL]: ['crmCreatedByName'],
+  [RolesEnum.CRM_HEAD]: ['crmCreatedByName', 'crmVerifiedByName'],
+  [RolesEnum.FINANCE_USER]: [
+    'crmCreatedByName',
+    'crmVerifiedByName',
+    'crmApprovedByName',
+  ],
+  [RolesEnum.FINANCE_HEAD]: [
+    'crmCreatedByName',
+    'crmVerifiedByName',
+    'crmApprovedByName',
+    'financeVerifiedByName',
+  ],
+  [RolesEnum.LOYALTY]: [
+    'crmCreatedByName',
+    'crmVerifiedByName',
+    'crmApprovedByName',
+    'financeVerifiedByName',
+    'financeApprovedByName',
+  ],
+};
 
 export const IOM_EXPORT_COLUMNS: IomExportColumn[] = [
   { header: 'ID', key: 'id', width: 10 },
@@ -20,6 +64,11 @@ export const IOM_EXPORT_COLUMNS: IomExportColumn[] = [
   { header: 'Unit No', key: 'unitNo', width: 12 },
   { header: 'Customer Name', key: 'customerName', width: 20 },
   { header: 'Sale Value', key: 'saleValue', width: 15 },
+  {
+    header: 'Sale Value Collected %',
+    key: 'saleValueCollectedPercentage',
+    width: 18,
+  },
   { header: 'Brokerage %', key: 'brokeragePercentage', width: 12 },
   { header: 'Total Brokerage', key: 'totalBrokerageAmount', width: 15 },
   { header: 'Referrer Points', key: 'referrerPoints', width: 15 },
@@ -73,13 +122,48 @@ export const IOM_EXPORT_COLUMNS: IomExportColumn[] = [
   { header: 'Invoice Updated At', key: 'invoiceUpdatedAt', width: 18 },
   { header: 'Invoice Req Number', key: 'invoiceReqNumber', width: 16 },
   { header: 'Points Updated At', key: 'pointsUpdatedAt', width: 18 },
+  {
+    header: 'Sale Value Amount Collected',
+    key: 'saleValueAmountCollected',
+    width: 20,
+  },
 ];
 
-export function resolveExportColumns(fields?: string[]): IomExportColumn[] {
+const COLUMN_BY_KEY = new Map(
+  IOM_EXPORT_COLUMNS.map((column) => [column.key, column]),
+);
+
+export function getRoleAllowedExportColumnKeys(role?: string): string[] {
+  const base = [...IOM_EXPORT_BASE_COLUMN_KEYS];
+
+  if (!role) {
+    return base;
+  }
+
+  const roleEnum = Object.values(RolesEnum).find((value) => value === role);
+  const additions = roleEnum
+    ? IOM_EXPORT_ROLE_COLUMN_KEYS[roleEnum]
+    : undefined;
+
+  if (!additions?.length) {
+    return base;
+  }
+
+  return [...base, ...additions];
+}
+
+export function resolveExportColumns(
+  fields?: string[],
+  role?: string,
+): IomExportColumn[] {
+  const allowedKeys = getRoleAllowedExportColumnKeys(role);
+  const allowedSet = new Set(allowedKeys);
+
   if (!fields?.length) {
-    return IOM_EXPORT_COLUMNS.filter(
-      (column) => !DEFAULT_EXPORT_EXCLUDED_KEYS.has(column.key),
-    );
+    // Empty intersection after role gate yields headers-only workbook.
+    return allowedKeys
+      .map((key) => COLUMN_BY_KEY.get(key))
+      .filter((column): column is IomExportColumn => column != null);
   }
 
   const knownKeys = new Set(IOM_EXPORT_COLUMNS.map((column) => column.key));
@@ -91,6 +175,8 @@ export function resolveExportColumns(fields?: string[]): IomExportColumn[] {
     );
   }
 
-  const fieldSet = new Set(fields);
-  return IOM_EXPORT_COLUMNS.filter((column) => fieldSet.has(column.key));
+  return fields
+    .filter((field) => allowedSet.has(field))
+    .map((field) => COLUMN_BY_KEY.get(field)!)
+    .filter(Boolean);
 }
diff --git a/src/modules/iom/dto/export-iom-excel.dto.ts b/src/modules/iom/dto/export-iom-excel.dto.ts
index 7100340c..056ececb 100644
--- a/src/modules/iom/dto/export-iom-excel.dto.ts
+++ b/src/modules/iom/dto/export-iom-excel.dto.ts
@@ -1,6 +1,41 @@
-import { IsArray, IsOptional, IsString } from 'class-validator';
+import { Transform } from 'class-transformer';
+import { IsArray, IsDate, IsInt, IsOptional, IsString } from 'class-validator';
+import { endOfDay } from 'date-fns';
 
 export class ExportIomExcelDto {
+  @IsOptional()
+  @IsString()
+  search?: string;
+
+  @IsOptional()
+  @IsString()
+  sortBy?: string;
+
+  @IsOptional()
+  @IsDate()
+  @Transform(({ value }) => (value ? new Date(value) : undefined))
+  startDate?: string;
+
+  @IsOptional()
+  @IsDate()
+  @Transform(({ value }) => (value ? endOfDay(value) : undefined))
+  endDate?: string;
+
+  @IsOptional()
+  @IsArray()
+  @IsString({ each: true })
+  iomStatus?: string[];
+
+  @IsOptional()
+  @IsArray()
+  @IsString({ each: true })
+  invoiceStatus?: string[];
+
+  @IsOptional()
+  @IsArray()
+  @IsInt({ each: true })
+  projects?: number[];
+
   @IsOptional()
   @IsArray()
   @IsString({ each: true })
diff --git a/src/modules/iom/dto/list-iom-listing.dto.spec.ts b/src/modules/iom/dto/list-iom-listing.dto.spec.ts
index 46f56ca7..b049fb02 100644
--- a/src/modules/iom/dto/list-iom-listing.dto.spec.ts
+++ b/src/modules/iom/dto/list-iom-listing.dto.spec.ts
@@ -43,4 +43,25 @@ describe('ListIomListingDto', () => {
     expect(instance.page).toBe(DEFAULT_PAGE);
     expect(instance.limit).toBe(DEFAULT_LIMIT);
   });
+
+  it('parses comma-separated projects into number array', () => {
+    const instance = plainToInstance(ListIomListingDto, {
+      projects: '10,11',
+    });
+    expect(instance.projects).toEqual([10, 11]);
+  });
+
+  it('returns undefined projects when omitted or empty', () => {
+    expect(plainToInstance(ListIomListingDto, {}).projects).toBeUndefined();
+    expect(
+      plainToInstance(ListIomListingDto, { projects: '' }).projects,
+    ).toBeUndefined();
+  });
+
+  it('filters invalid project tokens', () => {
+    const instance = plainToInstance(ListIomListingDto, {
+      projects: '10,abc,11',
+    });
+    expect(instance.projects).toEqual([10, 11]);
+  });
 });
diff --git a/src/modules/iom/dto/list-iom-listing.dto.ts b/src/modules/iom/dto/list-iom-listing.dto.ts
index f7d9b4b2..c8c32383 100644
--- a/src/modules/iom/dto/list-iom-listing.dto.ts
+++ b/src/modules/iom/dto/list-iom-listing.dto.ts
@@ -1,5 +1,6 @@
-import { IsIn, IsOptional, IsString } from 'class-validator';
+import { IsArray, IsIn, IsInt, IsOptional, IsString } from 'class-validator';
 import { CommonFindAllQueryDto } from 'src/helpers/dto/commonFindAll.dto';
+import { ToNumberArray } from 'src/utils/transformers';
 
 export class ListIomListingDto extends CommonFindAllQueryDto {
   @IsOptional()
@@ -12,4 +13,10 @@ export class ListIomListingDto extends CommonFindAllQueryDto {
 
   @IsOptional()
   invoiceStatus?: 'number';
+
+  @ToNumberArray()
+  @IsArray()
+  @IsOptional()
+  @IsInt({ each: true })
+  projects?: number[];
 }
diff --git a/src/modules/iom/services/iom-export.service.spec.ts b/src/modules/iom/services/iom-export.service.spec.ts
index 6a64f3c6..380c14ca 100644
--- a/src/modules/iom/services/iom-export.service.spec.ts
+++ b/src/modules/iom/services/iom-export.service.spec.ts
@@ -13,6 +13,8 @@ import { RolesEnum } from 'src/enums/roles.enum';
 import { IomExportService } from './iom-export.service';
 import { IomListingService } from './iom-listing.service';
 import { AuthenticatedUser } from './iom-validation.service';
+import { ExportIomExcelDto } from '../dto/export-iom-excel.dto';
+import { fromExportIomExcelDto } from '../mappers/iom-listing-filters.mapper';
 import { IomListItem } from '../types/iom-list-item.interface';
 
 jest.mock('src/common/helpers/excel.helper', () => ({
@@ -30,6 +32,11 @@ const CRM_USER: AuthenticatedUser = {
   crmProjects: [10],
 };
 
+const CRM_TL_USER: AuthenticatedUser = {
+  ...CRM_USER,
+  role: RolesEnum.CRM_TL,
+};
+
 const SAMPLE_ITEM: IomListItem = {
   id: 1,
   bookingId: 100,
@@ -38,6 +45,8 @@ const SAMPLE_ITEM: IomListItem = {
   unitNo: 'A-1201',
   customerName: 'Jane Doe',
   saleValue: 5_000_000,
+  saleValueCollectedPercentage: null,
+  saleValueAmountCollected: null,
   brokeragePercentage: 2.5,
   totalBrokerageAmount: 125_000,
   referrerPoints: 75_000,
@@ -79,7 +88,7 @@ const SAMPLE_ITEM: IomListItem = {
 
 describe('IomExportService', () => {
   let service: IomExportService;
-  const iomListingService = { findAllForExport: jest.fn() };
+  const iomListingService = { findIoms: jest.fn() };
   const awsService = { uploadToS3: jest.fn() };
   const configService = { get: jest.fn() };
 
@@ -92,7 +101,7 @@ describe('IomExportService', () => {
       return '';
     });
     configService.get.mockReturnValue('https://cdn.example.com/puravankara/');
-    iomListingService.findAllForExport.mockResolvedValue([SAMPLE_ITEM]);
+    iomListingService.findIoms.mockResolvedValue({ items: [SAMPLE_ITEM] });
     awsService.uploadToS3.mockResolvedValue(undefined);
 
     const module: TestingModule = await Test.createTestingModule({
@@ -107,20 +116,16 @@ describe('IomExportService', () => {
     service = module.get(IomExportService);
   });
 
-  it('exports with all columns by default and uploads to S3', async () => {
+  it('exports with role-based columns by default and uploads to S3', async () => {
     const result = await service.exportToExcel(CRM_USER, {});
 
-    expect(iomListingService.findAllForExport).toHaveBeenCalledWith(CRM_USER);
-    expect(generateExcelBuffer).toHaveBeenCalledWith(
-      resolveExportColumns(),
-      expect.any(Array),
-      'IOM Export',
+    expect(iomListingService.findIoms).toHaveBeenCalledWith(
+      CRM_USER,
+      {},
+      { skipPagination: true },
     );
-    expect(generateExcelBuffer).not.toHaveBeenCalledWith(
-      expect.arrayContaining([
-        expect.objectContaining({ key: 'statusCode' }),
-        expect.objectContaining({ key: 'crmVerifiedBy' }),
-      ]),
+    expect(generateExcelBuffer).toHaveBeenCalledWith(
+      resolveExportColumns(undefined, RolesEnum.CRM),
       expect.any(Array),
       'IOM Export',
     );
@@ -137,8 +142,27 @@ describe('IomExportService', () => {
     });
   });
 
+  it('forwards normalized listing filters to findIoms', async () => {
+    const dto = {
+      search: 'jane',
+      iomStatus: ['CRM_TL_APPROVAL_PENDING'],
+      invoiceStatus: ['1', '2'],
+      projects: [10],
+      startDate: new Date('2026-01-01'),
+      endDate: new Date('2026-01-31'),
+    } as unknown as ExportIomExcelDto;
+
+    await service.exportToExcel(CRM_USER, dto);
+
+    expect(iomListingService.findIoms).toHaveBeenCalledWith(
+      CRM_USER,
+      fromExportIomExcelDto(dto),
+      { skipPagination: true },
+    );
+  });
+
   it('filters columns when fields are provided', async () => {
-    await service.exportToExcel(CRM_USER, {
+    await service.exportToExcel(CRM_TL_USER, {
       fields: ['iomNo', 'projectName', 'statusLabel'],
     });
 
@@ -153,16 +177,13 @@ describe('IomExportService', () => {
     );
   });
 
-  it('includes excluded default columns when explicitly requested', async () => {
-    await service.exportToExcel(CRM_USER, {
-      fields: ['statusCode', 'crmVerifiedBy'],
+  it('omits role-disallowed fields without error', async () => {
+    await service.exportToExcel(CRM_TL_USER, {
+      fields: ['financeApprovedByName', 'iomNo'],
     });
 
     expect(generateExcelBuffer).toHaveBeenCalledWith(
-      [
-        expect.objectContaining({ key: 'statusCode' }),
-        expect.objectContaining({ key: 'crmVerifiedBy' }),
-      ],
+      [expect.objectContaining({ key: 'iomNo' })],
       expect.any(Array),
       'IOM Export',
     );
@@ -176,12 +197,12 @@ describe('IomExportService', () => {
   });
 
   it('still uploads when export list is empty', async () => {
-    iomListingService.findAllForExport.mockResolvedValueOnce([]);
+    iomListingService.findIoms.mockResolvedValueOnce({ items: [] });
 
     await service.exportToExcel(CRM_USER, {});
 
     expect(generateExcelBuffer).toHaveBeenCalledWith(
-      resolveExportColumns(),
+      resolveExportColumns(undefined, RolesEnum.CRM),
       [],
       'IOM Export',
     );
diff --git a/src/modules/iom/services/iom-export.service.ts b/src/modules/iom/services/iom-export.service.ts
index ef3db048..91dedc76 100644
--- a/src/modules/iom/services/iom-export.service.ts
+++ b/src/modules/iom/services/iom-export.service.ts
@@ -12,6 +12,7 @@ import { formatDateUtil } from 'src/helpers/date.helper';
 import { logger } from 'src/logger/logger';
 import { AwsService } from 'src/modules/aws/aws.service';
 import { ExportIomExcelDto } from '../dto/export-iom-excel.dto';
+import { fromExportIomExcelDto } from '../mappers/iom-listing-filters.mapper';
 import { IomListItem } from '../types/iom-list-item.interface';
 import { AuthenticatedUser } from './iom-validation.service';
 import { IomListingService } from './iom-listing.service';
@@ -41,8 +42,11 @@ export class IomExportService {
     dto: ExportIomExcelDto,
   ): Promise<{ data: { fileUrl: string; baseUrl: string } }> {
     try {
-      const columns = resolveExportColumns(dto.fields);
-      const items = await this.iomListingService.findAllForExport(user);
+      const columns = resolveExportColumns(dto.fields, user.role);
+      const filters = fromExportIomExcelDto(dto);
+      const { items } = await this.iomListingService.findIoms(user, filters, {
+        skipPagination: true,
+      });
       const rows = items.map((item) => this.toExportRow(item, columns));
       const buffer = await generateExcelBuffer(columns, rows, 'IOM Export');
 
diff --git a/src/modules/iom/services/iom-listing.service.spec.ts b/src/modules/iom/services/iom-listing.service.spec.ts
index c7232399..45fbece8 100644
--- a/src/modules/iom/services/iom-listing.service.spec.ts
+++ b/src/modules/iom/services/iom-listing.service.spec.ts
@@ -7,6 +7,7 @@ import { IomListingService } from './iom-listing.service';
 import { Iom } from '../entities/iom.entity';
 import { IomStatusCodeEnum } from '../enums/iom-status-code.enum';
 import { ListIomListingDto } from '../dto/list-iom-listing.dto';
+import { fromListIomListingDto } from '../mappers/iom-listing-filters.mapper';
 import { RolesEnum } from 'src/enums/roles.enum';
 import { AuthenticatedUser } from './iom-validation.service';
 import { ProjectUserMapping } from 'src/entities';
@@ -216,21 +217,137 @@ describe('IomListingService', () => {
     );
   });
 
-  it('findAllForExport applies role filter without pagination', async () => {
+  it('findAllForExport delegates to findIoms with skipPagination', async () => {
     const loyaltyUser: AuthenticatedUser = {
       ...CRM_USER,
       role: RolesEnum.LOYALTY,
     };
 
-    const items = await service.findAllForExport(loyaltyUser);
+    const items = await service.findAllForExport(loyaltyUser, {
+      search: 'jane',
+    });
 
     expect(qb.skip).not.toHaveBeenCalled();
     expect(qb.take).not.toHaveBeenCalled();
     expect(qb.getMany).toHaveBeenCalled();
+    expect(qb.getManyAndCount).not.toHaveBeenCalled();
+    expect(qb.andWhere).toHaveBeenCalledWith(
+      '(booking.customerName LIKE :like OR booking.bookingId LIKE :like OR booking.propertyNumber LIKE :like OR project.name LIKE :like OR i.iomNo LIKE :like)',
+      { like: '%jane%' },
+    );
     expect(items).toHaveLength(1);
     expect(items[0].projectName).toBe('Tower One');
   });
 
+  it('findIoms with skipPagination applies filters without pagination', async () => {
+    await service.findIoms(
+      CRM_USER,
+      {
+        search: 'tower',
+        iomStatus: IomStatusCodeEnum.CRM_TL_APPROVAL_PENDING,
+        startDate: new Date('2026-01-01'),
+        endDate: new Date('2026-01-31'),
+        invoiceStatus: 1,
+      } as unknown as ListIomListingDto,
+      { skipPagination: true },
+    );
+
+    expect(qb.skip).not.toHaveBeenCalled();
+    expect(qb.take).not.toHaveBeenCalled();
+    expect(qb.getMany).toHaveBeenCalled();
+    expect(qb.getManyAndCount).not.toHaveBeenCalled();
+    expect(qb.andWhere).toHaveBeenCalledWith(
+      '(booking.customerName LIKE :like OR booking.bookingId LIKE :like OR booking.propertyNumber LIKE :like OR project.name LIKE :like OR i.iomNo LIKE :like)',
+      { like: '%tower%' },
+    );
+    expect(qb.andWhere).toHaveBeenCalledWith('i.createdAt >= :startDate', {
+      startDate: new Date('2026-01-01'),
+    });
+    expect(qb.andWhere).toHaveBeenCalledWith(
+      'inv.status IN (:...invoiceStatuses)',
+      {
+        invoiceStatuses: ['1'],
+      },
+    );
+  });
+
+  it('scopes query to a single authorized project filter', async () => {
+    await service.findIoms(CRM_USER, {
+      projects: [10],
+    } as unknown as ListIomListingDto);
+
+    expect(qb.where).toHaveBeenCalledWith('i.projectId IN (:...crmProjects)', {
+      crmProjects: [10],
+    });
+  });
+
+  it('intersects projects filter with authorized projects', async () => {
+    await service.findIoms(CRM_USER, {
+      projects: [10, 99],
+    } as unknown as ListIomListingDto);
+
+    expect(qb.where).toHaveBeenCalledWith('i.projectId IN (:...crmProjects)', {
+      crmProjects: [10],
+    });
+  });
+
+  it('returns empty result for unauthorized project filter without querying', async () => {
+    const result = await service.findIoms(CRM_USER, {
+      projects: [999],
+    } as unknown as ListIomListingDto);
+
+    expect(result).toEqual({
+      items: [],
+      total: 0,
+      page: 1,
+      limit: 10,
+      totalPages: 0,
+    });
+    expect(qb.getManyAndCount).not.toHaveBeenCalled();
+  });
+
+  it('deduplicates project filter values', async () => {
+    await service.findIoms(CRM_USER, {
+      projects: [10, 10, 11],
+    } as unknown as ListIomListingDto);
+
+    expect(qb.where).toHaveBeenCalledWith('i.projectId IN (:...crmProjects)', {
+      crmProjects: [10, 11],
+    });
+  });
+
+  it('filters by multiple invoice statuses from normalized filters', async () => {
+    await service.findIoms(
+      CRM_USER,
+      {
+        invoiceStatuses: ['PENDING', 'APPROVED'],
+      },
+      { skipPagination: true },
+    );
+
+    expect(qb.andWhere).toHaveBeenCalledWith(
+      'inv.status IN (:...invoiceStatuses)',
+      { invoiceStatuses: ['PENDING', 'APPROVED'] },
+    );
+  });
+
+  it('normalizes comma-separated listing filters to same shape as export', () => {
+    const listingFilters = fromListIomListingDto({
+      iomStatus: `${IomStatusCodeEnum.CRM_TL_APPROVAL_PENDING},${IomStatusCodeEnum.IOM_CLOSED}`,
+      projects: [10, 11],
+      invoiceStatus: 1,
+    } as unknown as ListIomListingDto);
+
+    expect(listingFilters).toMatchObject({
+      iomStatuses: [
+        IomStatusCodeEnum.CRM_TL_APPROVAL_PENDING,
+        IomStatusCodeEnum.IOM_CLOSED,
+      ],
+      projects: [10, 11],
+      invoiceStatuses: ['1'],
+    });
+  });
+
   it('applies date range on createdAt', async () => {
     const startDate = new Date('2026-01-01');
     const endDate = new Date('2026-01-31T23:59:59.999Z');
@@ -288,7 +405,7 @@ describe('IomListingService', () => {
       referralPointsEdited: false,
       referralClassification: 'CLASS_A',
       statusCode: IomStatusCodeEnum.CRM_TL_APPROVAL_PENDING,
-      statusLabel: 'IOM Created',
+      statusLabel: 'CRM TL Approval Pending',
       iomCreatedAt: null,
       createdAt: new Date('2026-01-15T10:00:00Z'),
       iomPdfAvailable: true,
diff --git a/src/modules/iom/services/iom-listing.service.ts b/src/modules/iom/services/iom-listing.service.ts
index 237f6028..061894b0 100644
--- a/src/modules/iom/services/iom-listing.service.ts
+++ b/src/modules/iom/services/iom-listing.service.ts
@@ -7,10 +7,12 @@ import { ProjectUserMapping } from 'src/entities';
 import { Iom } from '../entities/iom.entity';
 import { IomStatusCodeEnum } from '../enums/iom-status-code.enum';
 import { ListIomListingDto } from '../dto/list-iom-listing.dto';
+import { fromListIomListingDto } from '../mappers/iom-listing-filters.mapper';
 import {
   IomListItem,
   IomListingResult,
 } from '../types/iom-list-item.interface';
+import { IomListingFilters } from '../types/iom-listing-filters.interface';
 import { AuthenticatedUser } from './iom-validation.service';
 import { toTitleCase } from 'src/helpers/stringHelper';
 import { getAllowedIomStatusesByRole } from '../utils/iom-role-status.util';
@@ -23,6 +25,10 @@ const ALLOWED_SORT_FIELDS: Record<string, string> = {
 };
 const basePath = '';
 
+export interface FindIomsOptions {
+  skipPagination?: boolean;
+}
+
 @Injectable()
 export class IomListingService {
   constructor(
@@ -34,47 +40,43 @@ export class IomListingService {
 
   async findIoms(
     user: AuthenticatedUser,
-    query: ListIomListingDto,
+    query: ListIomListingDto | IomListingFilters,
+    options?: FindIomsOptions,
   ): Promise<IomListingResult> {
-    const page = query.page ?? DEFAULT_PAGE;
-    const limit = query.limit ?? DEFAULT_LIMIT;
+    const skipPagination = options?.skipPagination === true;
+
+    const isListingDto = this.isListIomListingDto(query);
+    const page = isListingDto ? (query.page ?? DEFAULT_PAGE) : DEFAULT_PAGE;
+    const limit = isListingDto ? (query.limit ?? DEFAULT_LIMIT) : DEFAULT_LIMIT;
     const skip = (page - 1) * limit;
 
-    const crmProjects = await this.resolveUserProjects(user);
+    const filters = isListingDto ? fromListIomListingDto(query) : query;
+
+    const authorizedProjects = await this.resolveUserProjects(user);
 
-    if (crmProjects.length === 0) {
+    if (authorizedProjects.length === 0) {
       return { items: [], total: 0, page, limit, totalPages: 0 };
     }
 
-    let queryStatuses: IomStatusCodeEnum[] | undefined;
-
-    if (query.iomStatus) {
-      const validStatuses = Object.values(IomStatusCodeEnum);
-
-      queryStatuses = query.iomStatus
-        .split(',')
-        .map((s) => s.trim())
-        .filter(Boolean) as IomStatusCodeEnum[];
-
-      const invalid = queryStatuses.filter((s) => !validStatuses.includes(s));
+    const effectiveProjects = this.resolveEffectiveProjects(
+      filters.projects,
+      authorizedProjects,
+    );
 
-      if (invalid.length) {
-        throw new BadRequestException(
-          `Invalid iomStatus values: ${invalid.join(', ')}. Valid values: ${validStatuses.join(', ')}`,
-        );
-      }
+    if (effectiveProjects.length === 0) {
+      return { items: [], total: 0, page, limit, totalPages: 0 };
     }
 
     const effectiveStatuses = this.resolveEffectiveStatuses(
       user.role,
-      queryStatuses,
+      filters.iomStatuses,
     );
 
-    const qb = this.createBaseQueryBuilder(crmProjects);
-    this.applyListingFilters(qb, query, effectiveStatuses);
+    const qb = this.createBaseQueryBuilder(effectiveProjects);
+    this.applyListingFilters(qb, filters, effectiveStatuses);
 
-    if (query.sortBy) {
-      const [field, direction] = query.sortBy.split(':');
+    if (filters.sortBy) {
+      const [field, direction] = filters.sortBy.split(':');
       const dir = (direction || '').toUpperCase() === 'DESC' ? 'DESC' : 'ASC';
       const column = ALLOWED_SORT_FIELDS[field];
       if (column) {
@@ -84,7 +86,20 @@ export class IomListingService {
       qb.orderBy('i.createdAt', 'DESC');
     }
 
-    qb.skip(skip).take(limit);
+    if (!skipPagination) {
+      qb.skip(skip).take(limit);
+    }
+
+    if (skipPagination) {
+      const rows = await qb.getMany();
+      return {
+        items: rows.map((iom) => this.toListItem(iom)),
+        total: rows.length,
+        page: 1,
+        limit: rows.length,
+        totalPages: rows.length > 0 ? 1 : 0,
+      };
+    }
 
     const [rows, total] = await qb.getManyAndCount();
 
@@ -97,26 +112,46 @@ export class IomListingService {
     };
   }
 
-  async findAllForExport(user: AuthenticatedUser): Promise<IomListItem[]> {
-    const crmProjects = await this.resolveUserProjects(user);
+  async findAllForExport(
+    user: AuthenticatedUser,
+    query: ListIomListingDto = {},
+  ): Promise<IomListItem[]> {
+    const { items } = await this.findIoms(user, query, {
+      skipPagination: true,
+    });
+    return items;
+  }
 
-    if (crmProjects.length === 0) {
-      return [];
+  private isListIomListingDto(
+    query: ListIomListingDto | IomListingFilters,
+  ): query is ListIomListingDto {
+    if ('iomStatuses' in query || 'invoiceStatuses' in query) {
+      return false;
     }
 
-    const effectiveStatuses = this.resolveEffectiveStatuses(user.role);
-    const qb = this.createBaseQueryBuilder(crmProjects);
+    if ('iomStatus' in query && Array.isArray(query.iomStatus)) {
+      return false;
+    }
 
-    if (effectiveStatuses?.length) {
-      qb.andWhere('status.code IN (:...effectiveStatuses)', {
-        effectiveStatuses,
-      });
+    if ('invoiceStatus' in query && Array.isArray(query.invoiceStatus)) {
+      return false;
     }
 
-    qb.orderBy('i.createdAt', 'DESC');
+    return true;
+  }
+
+  private resolveEffectiveProjects(
+    requestedProjects: number[] | undefined,
+    authorizedProjects: number[],
+  ): number[] {
+    if (!requestedProjects?.length) {
+      return authorizedProjects;
+    }
 
-    const rows = await qb.getMany();
-    return rows.map((iom) => this.toListItem(iom));
+    const authorizedSet = new Set(authorizedProjects);
+    return [...new Set(requestedProjects)].filter((projectId) =>
+      authorizedSet.has(projectId),
+    );
   }
 
   private resolveEffectiveStatuses(
@@ -172,11 +207,11 @@ export class IomListingService {
 
   private applyListingFilters(
     qb: SelectQueryBuilder<Iom>,
-    query: ListIomListingDto,
+    filters: IomListingFilters,
     effectiveStatuses?: IomStatusCodeEnum[],
   ): void {
-    if (query.search) {
-      const like = `%${query.search}%`;
+    if (filters.search) {
+      const like = `%${filters.search}%`;
       qb.andWhere(
         '(booking.customerName LIKE :like OR booking.bookingId LIKE :like OR booking.propertyNumber LIKE :like OR project.name LIKE :like OR i.iomNo LIKE :like)',
         { like },
@@ -189,17 +224,19 @@ export class IomListingService {
       });
     }
 
-    if (query.startDate) {
-      qb.andWhere('i.createdAt >= :startDate', { startDate: query.startDate });
+    if (filters.startDate) {
+      qb.andWhere('i.createdAt >= :startDate', {
+        startDate: filters.startDate,
+      });
     }
 
-    if (query.endDate) {
-      qb.andWhere('i.createdAt <= :endDate', { endDate: query.endDate });
+    if (filters.endDate) {
+      qb.andWhere('i.createdAt <= :endDate', { endDate: filters.endDate });
     }
 
-    if (query.invoiceStatus) {
-      qb.andWhere('inv.status = :invoiceStatus', {
-        invoiceStatus: query.invoiceStatus,
+    if (filters.invoiceStatuses?.length) {
+      qb.andWhere('inv.status IN (:...invoiceStatuses)', {
+        invoiceStatuses: filters.invoiceStatuses,
       });
     }
   }
@@ -249,6 +286,8 @@ export class IomListingService {
       unitNo: iom.booking?.propertyNumber ?? null,
       customerName: this.resolveCustomerName(iom),
       saleValue: iom.salePrice,
+      saleValueCollectedPercentage: null,
+      saleValueAmountCollected: null,
       brokeragePercentage: iom.brokeragePercentage,
       totalBrokerageAmount: iom.totalBrokerageAmount,
       referrerPoints: iom.referrerPoints,
diff --git a/src/modules/iom/types/iom-list-item.interface.ts b/src/modules/iom/types/iom-list-item.interface.ts
index 70cacdf4..7f8f7bbe 100644
--- a/src/modules/iom/types/iom-list-item.interface.ts
+++ b/src/modules/iom/types/iom-list-item.interface.ts
@@ -6,6 +6,8 @@ export interface IomListItem {
   unitNo: string | null;
   customerName: string | null;
   saleValue: number;
+  saleValueCollectedPercentage: number | null;
+  saleValueAmountCollected: number | null;
   brokeragePercentage: number;
   totalBrokerageAmount: number;
   referrerPoints: number;

## Staged Diff

(No staged index changes.)
