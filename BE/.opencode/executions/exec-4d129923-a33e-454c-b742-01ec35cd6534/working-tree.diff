# Working Tree Diff
# Includes unstaged and staged changes.
# Generated by iProgrammer Flow for execution exec-4d129923-a33e-454c-b742-01ec35cd6534.

## Unstaged Diff
diff --git a/docs/ai/stories/PN-39/implementation-plan.md b/docs/ai/stories/PN-39/implementation-plan.md
index 3d7ffbd3..a4ab5474 100644
--- a/docs/ai/stories/PN-39/implementation-plan.md
+++ b/docs/ai/stories/PN-39/implementation-plan.md
@@ -1,333 +1,221 @@
-# PN-39 Implementation Plan: CRM Team Availability (Parts 2–3)
+# PN-39 Implementation Plan: `markAvailable` Soft-Cancel Logic
 
 ## Summary
 
-**Part 1 is already implemented** (`POST /api/users/availability/unavailable`, entity, DTO, service, module wiring, unit tests). This execution delivers the **remaining net-new work**:
+Replace the current **collapse-based** upcoming-window cancellation in `UserAvailabilityService.markAvailable` with **soft-cancel** semantics. Active windows still end by setting `unavailable_to = now`. Upcoming windows are cancelled by setting `cancelled_at = now` and `cancelled_by = loggedInUser.dbId` while preserving original `unavailable_from` / `unavailable_to`. No rows are deleted; past windows are never mutated.
 
-| Part | Endpoint | Status |
-|------|----------|--------|
-| 1 | `POST /api/users/availability/unavailable` | Done — re-validate only |
-| 2 | `POST /api/users/availability/available` | **To implement** |
-| 3 | `GET /api/users/team/availability` | **To implement** |
+**Prerequisite:** `cancelled_at` and `cancelled_by` columns do not exist on `user_availability` today (entity or migration). Add them before updating service logic.
 
-All availability logic stays in `src/modules/users/`. Do **not** modify the IOM module; read `ioms` and `project_user_mapping` via injected repositories only.
-
----
-
-## Current State (confirmed)
-
-| Artifact | Location | Notes |
-|----------|----------|-------|
-| Entity | `src/modules/users/entities/user-availability.entity.ts` | Maps `user_availability`; no migration needed |
-| Part 1 DTO | `src/modules/users/dto/mark-unavailable.dto.ts` | `userId`, `unavailableFrom`, `unavailableTo`, `reason?` |
-| Service (Part 1) | `src/modules/users/services/user-availability.service.ts` | `markUnavailable()` with overlap rejection (`ConflictException`) |
-| TL access helper | `src/modules/users/user.service.ts` → `validateTLAccess()` | Uses `reporting_to` (`reportingTo`), CRM role check, forbids self-action |
-| Controller (Part 1) | `src/modules/users/user.controller.ts` L277–285 | `RmAdminAuthGuard` + `RolesGuard` + `@Roles(CRM_TL)` |
-| Module wiring | `src/modules/users/user.module.ts` | `UserAvailability` entity + `UserAvailabilityService` registered |
-| Tests | `user-availability.service.spec.ts`, `user.service.spec.ts` | Part 1 + `validateTLAccess` covered |
-
-**Resolved open questions from spec:**
-
-- **Team field:** `users.reporting_to` → `Users.reportingTo`
-- **Auth guards:** `RmAdminAuthGuard` + `RolesGuard` + `@Roles(RolesEnum.CRM_TL)`
-- **Entity location:** `UserAvailability` already lives under Users module (also imported by IOM assignment service — do not duplicate)
-- **Overlap on insert:** Already rejected in Part 1; display logic still resolves latest active window defensively
-- **`empId`:** Map from `Users.empCode` (`emp_code` column)
-- **`pendingIomsCount`:** Omit — not straightforward without workflow semantics
-
----
+**Out of scope:** `markUnavailable`, `getTeamAvailability`, controller, DTOs, and all other modules.
 
 ## Target Files
 
-### Edit
-
-- `src/modules/users/user.controller.ts` — add Part 2 + Part 3 endpoints
-- `src/modules/users/services/user-availability.service.ts` — add `markAvailable()`, `getTeamAvailability()`, private helpers
-- `src/modules/users/user.module.ts` — register `Iom`, `ProjectUserMapping` in `TypeOrmModule.forFeature` (entity-only; no `IomModule` import)
-- `src/modules/users/services/user-availability.service.spec.ts` — tests for new service methods
+| File | Action |
+|------|--------|
+| `src/modules/users/entities/user-availability.entity.ts` | **Edit** — add `cancelledAt`, `cancelledBy` columns (+ optional `cancelledByUser` relation) |
+| `src/migrations/<timestamp>-AddCancelledColumnsToUserAvailability.ts` | **Create** — `ALTER TABLE user_availability` add nullable `cancelled_at`, `cancelled_by` |
+| `src/modules/users/services/user-availability.service.ts` | **Edit** — refactor resolver + `markAvailable` soft-cancel branch |
+| `src/modules/users/services/user-availability.service.spec.ts` | **Edit** — update/replace collapse tests; add soft-cancel coverage |
 
-### Create
+**Verify only (no changes expected):**
 
-- `src/modules/users/dto/mark-available.dto.ts` — `{ userId: number }`
-- `src/modules/users/dto/team-availability.dto.ts` — response typing (`TeamMemberAvailabilityDto`, `TeamProjectDto`, `AvailabilityStatus` enum or union)
-
-### Do not edit (unless acceptance gap found in Part 1)
-
-- `src/modules/users/entities/user-availability.entity.ts`
-- `src/modules/users/dto/mark-unavailable.dto.ts`
-- `src/modules/users/user.service.ts` (`validateTLAccess` is sufficient)
-- IOM module files
-
----
+- `src/modules/users/user.controller.ts`
+- `src/modules/users/dto/mark-available.dto.ts`
+- `src/modules/users/user.module.ts`
 
 ## Context Budget
 
-Implementer MUST:
-
-1. **Inspect target files first** — start with `user-availability.service.ts`, `user.controller.ts`, `user.module.ts`; do not broad-scan the repo.
-2. **Open non-target files only when required** — allowed references:
-   - `src/modules/users/entities/user.entity.ts` (field names)
-   - `src/modules/users/user.service.ts` (`validateTLAccess` signature)
-   - `src/modules/iom/entities/iom.entity.ts` (`assignedTo`, `deletedAt`)
-   - `src/modules/masters/projects/entities/project_user_mapping.entity.ts` (project resolution)
-   - `src/modules/iom/services/iom-listing.service.ts` (`resolveUserProjects` pattern, lines ~133–156)
-   - `src/modules/iom/services/iom-assignment.service.ts` (`loadUnavailableUserIds` active-window query, lines ~143–151)
-   - `src/enums/roles.enum.ts` (`CRM`, `CRM_TL`)
-3. **Use provider-native edit tools** — do not paste full file contents, full diffs, or large code blocks in chat.
-4. **Run only targeted validation** — unit tests for changed specs + `npm run build`; skip full-suite `test:e2e` unless regressions appear.
-
----
-
-## Implementation Steps
+- Open **target files first**; do not broad-scan `src/modules/users/` or the repo.
+- Open non-target files only for direct imports, callers, tests, or required config (e.g. voucher entity migration at `src/migrations/1756474247362-addCancelReasonInVoucher.ts` for column naming/style reference).
+- Use native edit tools; do not paste full file contents or large diffs in chat.
+- Run only the validation commands listed below.
 
-### Step 0 — Verify Part 1 (no code unless gap)
+## Current State
 
-Skim existing Part 1 against acceptance criteria AC 1–7. Only fix if a clear gap exists (e.g., missing guard). Do not refactor overlap logic.
+### Service (`user-availability.service.ts`)
 
----
+`markAvailable` already handles active and upcoming windows via `resolveActiveOrUpcomingWindow`, but:
 
-### Step 1 — Create DTOs
+1. Resolver **does not** filter `cancelled_at IS NULL`.
+2. Upcoming branch **collapses** dates (`unavailable_from = now`, `unavailable_to = now`) — this **must be replaced** by soft-cancel.
+3. Resolver should be renamed to `resolveRelevantWindow` per spec.
 
-**`mark-available.dto.ts`**
-
-```ts
-{ userId: number }  // @IsNumber()
+```86:114:src/modules/users/services/user-availability.service.ts
+  async markAvailable(...) {
+    // validateTLAccess → resolveActiveOrUpcomingWindow → branch on unavailableFrom <= now
+    // upcoming branch currently sets unavailableFrom + unavailableTo to now  ← WRONG per change request
+  }
 ```
 
-**`team-availability.dto.ts`** — define response shapes (class or interface exported for service return type):
+### Entity / DB
 
-| Field | Type | Notes |
-|-------|------|-------|
-| `userId` | number | `Users.id` |
-| `empId` | string \| null | from `empCode` |
-| `name` | string | |
-| `email` | string | |
-| `role` | string | `RolesEnum.CRM` |
-| `currentStatus` | `'AVAILABLE' \| 'UNAVAILABLE'` | |
-| `unavailableFrom` | string (ISO)? | omit when AVAILABLE |
-| `unavailableTo` | string (ISO)? | omit when AVAILABLE |
-| `projects` | `{ id: number; name: string }[]` | |
-| `allocatedIomsCount` | number | |
+`UserAvailability` entity has `markedBy`, `reason`, window dates — **no** `cancelledAt` / `cancelledBy`. Base migration `1781264100000-CreateUserAvailability.ts` creates table without soft-cancel columns.
 
----
+### Tests (`user-availability.service.spec.ts`)
 
-### Step 2 — Extend `UserAvailabilityService`
+`describe('markAvailable')` tests assert collapse behavior for upcoming windows (lines ~329–450). These must be rewritten for soft-cancel. Active-window and already-available tests are largely correct but should assert resolver includes `cancelled_at IS NULL`.
 
-Inject additional repositories (constructor):
+## Implementation Steps
 
-- `@InjectRepository(Users)` — team member query
-- `@InjectRepository(Iom)` — IOM counts
-- `@InjectRepository(ProjectUserMapping)` — project list
+### Step 1 — Add soft-cancel columns (entity + migration)
 
-Import entities from `src/entities` barrel to match repo conventions.
+**Entity** (`user-availability.entity.ts`):
 
-#### 2a. `markAvailable(loggedInUser, dto: MarkAvailableDto)`
+Add nullable columns following existing snake_case / camelCase conventions:
 
-1. `await this.userService.validateTLAccess(loggedInUser.dbId, dto.userId)`
-2. `const now = new Date()`
-3. Find **latest active** window:
+```typescript
+@Column({ name: 'cancelled_at', type: 'datetime', nullable: true })
+cancelledAt: Date | null;
 
-```sql
-WHERE user_id = :userId
-  AND unavailable_from <= :now
-  AND unavailable_to >= :now
-ORDER BY unavailable_from DESC   -- latest active wins
-LIMIT 1
+@Column({ name: 'cancelled_by', type: 'int', nullable: true })
+cancelledBy: number | null;
 ```
 
-Use QueryBuilder on `UserAvailability`.
-
-4. If none found → `BadRequestException('No active unavailability window found')`
-5. `await availabilityRepo.update(id, { unavailableTo: now })` — only mutates active window; past windows untouched
-6. Return updated record (or `{ success: true }` per nearby controller patterns — match Part 1 which returns the saved entity)
-
-#### 2b. Private helper: `resolveActiveWindow(userId, now)`
-
-Extract reusable QueryBuilder used by both `markAvailable` and `getTeamAvailability`:
-
-- Returns `{ unavailableFrom, unavailableTo } | null`
-- Condition: `unavailable_from <= now AND unavailable_to >= now`
-- Order: `unavailableFrom DESC`, `getOne()`
+Optional: add `@ManyToOne(() => Users)` + `@JoinColumn({ name: 'cancelled_by' })` for `cancelledByUser` (mirror `markedByUser` pattern). Not required for `markAvailable` to work.
 
-Mirror the active-window predicate already used in `iom-assignment.service.ts` `loadUnavailableUserIds`.
+**Migration** (new file, timestamp after `1781510857241`):
 
-#### 2c. `getTeamAvailability(loggedInUser)`
-
-1. Load direct-report CRM users:
-
-```ts
-usersRepo.createQueryBuilder('u')
-  .innerJoin('u.role', 'role')
-  .where('u.reporting_to = :tlId', { tlId: loggedInUser.dbId })
-  .andWhere('role.name = :crmRole', { crmRole: RolesEnum.CRM })
-  .select(['u.id', 'u.name', 'u.email', 'u.empCode'])
-  .getMany()
+```sql
+ALTER TABLE user_availability
+  ADD COLUMN cancelled_at DATETIME NULL COMMENT 'When a future window was soft-cancelled',
+  ADD COLUMN cancelled_by INT NULL COMMENT 'TL who soft-cancelled the window',
+  ADD CONSTRAINT fk_user_availability_cancelled_by
+    FOREIGN KEY (cancelled_by) REFERENCES users(id) ON DELETE RESTRICT
 ```
 
-2. If empty → return `[]`
-
-3. Collect `userIds` from results.
-
-4. **Batch queries** (avoid N+1):
-
-   **a) Active availability** — single query for all team user IDs with active windows; group in memory by `userId`, pick latest `unavailableFrom` per user when multiple rows.
+`down`: drop FK, then drop both columns. Match `DATETIME` style from `CreateUserAvailability` migration (not `DATETIME(3)` unless team prefers voucher-style precision).
 
-   **b) Projects** — QueryBuilder on `ProjectUserMapping`:
+### Step 2 — Replace resolver with `resolveRelevantWindow`
 
-   ```ts
-   .innerJoinAndSelect('pum.project', 'project')
-   .where('pum.user_id IN (:...userIds)', { userIds })
-   .andWhere('pum.removed_at IS NULL')
-   ```
+Rename `resolveActiveOrUpcomingWindow` → `resolveRelevantWindow` and add `cancelled_at IS NULL` filter:
 
-   Group by user → `{ id, name }[]` (dedupe project ids). Follow `iom-listing.service.ts` `resolveUserProjects` semantics (`removedAt: IsNull()`).
-
-   **c) Allocated IOM counts** — QueryBuilder on `Iom`:
-
-   ```ts
-   .select('iom.assigned_to', 'userId')
-   .addSelect('COUNT(*)', 'count')
-   .where('iom.assigned_to IN (:...userIds)', { userIds })
-   .andWhere('iom.deleted_at IS NULL')
-   .groupBy('iom.assigned_to')
-   .getRawMany()
-   ```
-
-5. Map each team member to `TeamMemberAvailabilityDto`:
-   - `currentStatus`: `UNAVAILABLE` if active window exists, else `AVAILABLE`
-   - When `UNAVAILABLE`: include `unavailableFrom` / `unavailableTo` as `.toISOString()`
-   - When `AVAILABLE`: **omit** those fields (do not send `null` unless a nearby Users endpoint convention requires it — check one existing GET in `user.controller.ts` and match)
-   - `allocatedIomsCount`: from grouped count or `0`
-   - `projects`: from grouped mappings or `[]`
-
-6. Return array (transform interceptor wraps in standard envelope).
-
----
-
-### Step 3 — Controller endpoints
-
-Add to `user.controller.ts` (same guard pattern as Part 1):
-
-```ts
-@Post('availability/available')
-@UseGuards(RmAdminAuthGuard, RolesGuard)
-@Roles(RolesEnum.CRM_TL)
-async markAvailable(@User() user: { dbId: number }, @Body() dto: MarkAvailableDto) {
-  return this.userAvailabilityService.markAvailable(user, dto);
-}
-
-@Get('team/availability')
-@UseGuards(RmAdminAuthGuard, RolesGuard)
-@Roles(RolesEnum.CRM_TL)
-async getTeamAvailability(@User() user: { dbId: number }) {
-  return this.userAvailabilityService.getTeamAvailability(user);
+```typescript
+private async resolveRelevantWindow(
+  userId: number,
+  now: Date,
+): Promise<UserAvailability | null> {
+  return this.availabilityRepo
+    .createQueryBuilder('ua')
+    .where('ua.user_id = :userId', { userId })
+    .andWhere('ua.cancelled_at IS NULL')
+    .andWhere('ua.unavailable_to >= :now', { now })
+    .orderBy('ua.unavailable_from', 'ASC')
+    .getOne();
 }
 ```
 
-Place `GET team/availability` **before** `@Get(':id')` if route-order issues arise during manual testing (NestJS `:id` is single-segment, so likely safe either way).
-
----
-
-### Step 4 — Module wiring
-
-In `user.module.ts`, add to `TypeOrmModule.forFeature([...])`:
+**Semantics:** earliest non-cancelled window with `unavailable_to >= now`. Classification stays in `markAvailable`.
 
-- `Iom` (from `src/entities`)
-- `ProjectUserMapping` (from `src/entities`)
+### Step 3 — Refactor `markAvailable` decision flow
 
-Do **not** import `IomModule` — entity registration only prevents circular dependencies.
+Keep existing structure; change only the upcoming branch and add brief inline comments:
 
----
+```
+markAvailable(loggedInUser, dto):
+  1. await validateTLAccess(loggedInUser.dbId, dto.userId)   // unchanged
+  2. now = new Date()
+  3. window = await resolveRelevantWindow(dto.userId, now)
+  4. if !window → throw BadRequestException('User is already available')
+  5. if window.unavailableFrom <= now:
+       // Active: end window early — do NOT touch cancelled_at/cancelled_by/unavailable_from
+       update { unavailableTo: now }
+     else:
+       // Upcoming: soft-cancel — preserve unavailable_from and unavailable_to
+       update { cancelledAt: now, cancelledBy: loggedInUser.dbId }
+  6. return findOne({ where: { id: window.id } })
+```
 
-### Step 5 — Unit tests (`user-availability.service.spec.ts`)
+**Hard constraints (do not violate):**
 
-Extend existing test module with mocked `Users`, `Iom`, `ProjectUserMapping` repositories.
+| Constraint | Rule |
+|------------|------|
+| Never update `unavailable_from` | Active branch updates only `unavailableTo`; upcoming branch updates only cancel fields |
+| Never delete records | Use `update` + `findOne`, not `delete` |
+| Never touch past windows | Resolver filters `unavailable_to >= now` |
+| Ignore already-cancelled | Resolver filters `cancelled_at IS NULL` |
+| Active takes precedence | Single earliest relevant window; if active, upcoming siblings untouched |
+| Scope isolation | Do not modify `markUnavailable`, `getTeamAvailability`, or their helpers |
 
-| Test case | Method | Assertion |
-|-----------|--------|-----------|
-| Ends active window | `markAvailable` | `validateTLAccess` called; `update` sets `unavailableTo ≈ now` |
-| No active window | `markAvailable` | `BadRequestException` |
-| Forbidden (not on team) | `markAvailable` | `ForbiddenException` from `validateTLAccess` |
-| Returns team members | `getTeamAvailability` | correct shape, only `reporting_to = tl` CRM users |
-| AVAILABLE user | `getTeamAvailability` | no `unavailableFrom`/`unavailableTo` |
-| UNAVAILABLE user | `getTeamAvailability` | status + window fields present |
-| Zero IOMs | `getTeamAvailability` | `allocatedIomsCount: 0` |
-| Projects mapped | `getTeamAvailability` | `{ id, name }[]` from `ProjectUserMapping` |
-| Empty team | `getTeamAvailability` | `[]` |
+### Step 4 — Update unit tests
 
-Reuse existing mock patterns from the Part 1 spec file.
+In `describe('markAvailable')`:
 
----
+| Test | Action |
+|------|--------|
+| Active window end | Keep; assert update payload has only `unavailableTo`, not `cancelledAt`/`cancelledBy`/`unavailableFrom` |
+| Already available (null resolver) | Keep message `'User is already available'`; assert no `update` |
+| Upcoming soft-cancel | **Replace** collapse test: assert `update` called with `{ cancelledAt: Date, cancelledBy: tlUser.dbId }` and **not** `unavailableFrom`/`unavailableTo` |
+| Active does not modify `unavailableFrom` | Keep |
+| Earliest upcoming window | **Update** assertions to soft-cancel fields, not collapse |
+| No delete on cancel | Keep; still `update` + `findOne`, no `delete` |
+| Forbidden on TL denial | Keep |
+| **Add:** cancelled windows excluded | Resolver returns null when only rows have `cancelledAt` set → `User is already available` |
+| **Add:** past windows only | Resolver returns null → `User is already available`, no mutation |
+| **Add:** active-over-upcoming precedence | Mock active window as earliest relevant; assert only `unavailableTo` updated |
+| **Add:** original dates preserved on soft-cancel | After update, `findOne` returns window with original `unavailableFrom`/`unavailableTo` unchanged |
 
-### Step 6 — Sample curl commands (add as code comments in controller or brief note in PR description)
+When mocking query builder, optionally assert `andWhere` was called with `cancelled_at IS NULL` (if mock setup allows).
 
-```bash
-# Part 2 — Mark available
-curl -X POST "$BASE/api/users/availability/available" \
-  -H "Authorization: Bearer $TOKEN" \
-  -H "Content-Type: application/json" \
-  -d '{"userId": 101}'
-
-# Part 3 — Team availability
-curl -X GET "$BASE/api/users/team/availability" \
-  -H "Authorization: Bearer $TOKEN"
-```
+**Do not** change `markUnavailable` or `getTeamAvailability` test blocks.
 
-Replace `$BASE` with `http://localhost:PORT/api` (or `api/{NODE_ENV}` in non-prod).
+### Step 5 — Smoke-check non-target surfaces
 
----
+Confirm no import/signature breakage in `user.controller.ts`. No controller changes needed — endpoint contract unchanged.
 
 ## Validation Commands
 
-Run in order after implementation:
+Run from repo root after changes:
 
 ```bash
-npm run test -- src/modules/users/services/user-availability.service.spec.ts
-npm run test -- src/modules/users/user.service.spec.ts
+# Unit tests for changed service only
+npm run test -- --testPathPattern=user-availability.service.spec
+
+# Lint changed files
+npm run lint
+
+# Build (catches entity/type errors)
 npm run build
-npm run lint -- --max-warnings=0 src/modules/users/user.controller.ts src/modules/users/services/user-availability.service.ts src/modules/users/user.module.ts src/modules/users/dto/mark-available.dto.ts src/modules/users/dto/team-availability.dto.ts
 ```
 
-Optional if time permits:
+If migration is added locally and DB is available:
 
 ```bash
-npm run test -- src/modules/users/user.controller.spec.ts
+npm run migration:run
 ```
 
----
-
 ## Acceptance Criteria Mapping
 
-| AC | Requirement | Step |
-|----|-------------|------|
-| 8–10 | Mark available | Steps 2a, 3, 5 |
-| 11–18 | Team listing | Steps 2b–2c, 3, 5 |
-| 19–21 | Users module only, no frontend/cron, standard envelope | Steps 2–4 (entity-only IOM reads) |
-
-Part 1 AC 1–7: Step 0 verification only.
-
----
-
-## Risks & Mitigations
+| # | Criterion | How to verify |
+|---|-----------|---------------|
+| 1 | Active window ends at `now` | Unit test: `unavailableTo` updated; `cancelledAt`/`cancelledBy` null |
+| 2 | Upcoming soft-cancelled | Unit test: `cancelledAt ≈ now`, `cancelledBy = TL dbId`; dates unchanged |
+| 3 | Already available → 400 | Unit test: exact message `User is already available` |
+| 4 | Active over upcoming | Unit test: active window mocked first; upcoming untouched |
+| 5 | Earliest upcoming only | Unit test: resolver returns earliest; single `update` |
+| 6 | Past windows untouched | Unit test: resolver null; no `update` |
+| 7 | Cancelled windows ignored | Unit test: `cancelled_at IS NULL` in resolver |
+| 8 | TL auth preserved | Existing `ForbiddenException` test |
+| 9 | Scope limited | No edits outside target files |
+| 10 | Full test coverage | All branches in `markAvailable` covered |
+
+## Risks
 
 | Risk | Mitigation |
 |------|------------|
-| Circular import Users ↔ IOM | Register `Iom` / `ProjectUserMapping` entities only; never import `IomModule` or IOM services |
-| N+1 queries on team listing | Batch availability, projects, and IOM counts by `userIds` |
-| `crm_projects` JSON vs `project_user_mapping` | Use `ProjectUserMapping` with `removedAt IS NULL` — matches IOM listing behavior |
-| Datetime timezone drift | Use `new Date()` consistently; return ISO strings via `.toISOString()` |
-| Route shadowing by `:id` | Register `team/availability` before parameterized GET if needed |
-| Part 1 overlap rejection vs display "latest wins" | Overlaps unlikely on insert; still order active windows DESC in read path |
-
----
+| **Listing may show soft-cancelled users as unavailable** | `getTeamAvailability` / `loadActiveWindowsForUsers` do not filter `cancelled_at`. Per spec this is **out of scope** for PN-39; note as follow-up if dashboard accuracy is required. |
+| **Overlap check in `markUnavailable` ignores `cancelled_at`** | Pre-existing; do not change in this story. Soft-cancelled future windows may still block new overlapping windows until a follow-up. |
+| **Migration on shared DB** | Coordinate deploy order: migration before code that writes `cancelled_at`. |
+| **Test mocks stale after rename** | Grep spec file for `resolveActiveOrUpcomingWindow` / collapse assertions and update all. |
 
 ## Assumptions
 
-1. Part 1 implementation is correct and stable; no functional changes unless Step 0 finds a gap.
-2. Team membership = `users.reporting_to = authenticated TL id` + target role `CRM`.
-3. `allocatedIomsCount` = count of non-deleted IOMs where `assigned_to = user.id`.
-4. `projects` = active rows in `project_user_mapping` joined to `projects` for `id` and `name`.
-5. `pendingIomsCount` is **out of scope** for this execution.
-6. `iom_assignment_state` is not needed for Part 3 response (round-robin state only).
-7. No new migration — existing `user_availability` schema is sufficient.
-8. Response envelope is handled by `src/interceptors/transform.interceptor.ts`; return plain data from controller/service.
+1. `cancelled_at` and `cancelled_by` must be **added** via new migration; they are not in production schema yet.
+2. `cancelled_by` stores `loggedInUser.dbId` (same as `markedBy` on create).
+3. Error message for no relevant window is exactly **`User is already available`** (already implemented).
+4. `resolveRelevantWindow` naming supersedes `resolveActiveOrUpcomingWindow`.
+5. Soft-cancel **supersedes** the prior collapse approach (`unavailable_from = now`, `unavailable_to = now`); remove that branch entirely.
+6. `markUnavailable` overlap query and `getTeamAvailability` active-window loader are intentionally **not** updated per story constraints.
+
+## Supersedes
+
+This plan **replaces** the prior implementation plan section that described upcoming-window cancellation via date collapse. All upstream requirements from the 2026-06-16 change request and `docs/ai/stories/PN-39/spec.md` take precedence.
diff --git a/docs/ai/stories/PN-39/spec.md b/docs/ai/stories/PN-39/spec.md
index 32ef6329..a940ddef 100644
--- a/docs/ai/stories/PN-39/spec.md
+++ b/docs/ai/stories/PN-39/spec.md
@@ -1,342 +1,268 @@
-# PN-39: List Team Members of CRM TL (CRM User Availability)
+# PN-39: Modification in User Availability — `markAvailable` (Soft-Cancel)
 
 ## Overview
 
-Implement backend-only CRM user availability management in the **Users module** of the NestJS + TypeORM application. CRM Team Leads (`CRM_TL`) can:
+Extend the **mark available** flow so a CRM Team Lead (CRM_TL) can restore a team member’s availability when they have either an **active** unavailability window or a **scheduled (upcoming)** one. Today, `markAvailable` only ends active windows; upcoming windows cause a `400 Bad Request`. This story updates only that logic using **soft-cancel** semantics: preserve historical accuracy by **never deleting** records and **never mutating** `unavailable_from` or past completed windows.
 
-1. Mark direct-report CRM users **unavailable** for datetime windows.
-2. Optionally mark a user **available** again by ending the current active unavailability window.
-3. **List team members** with real-time availability status and CRM workload context (projects, IOM counts).
+## Background
 
-Availability logic **must** live in the Users module — not the IOM module. No frontend or cron dependencies.
+User availability is stored as time-bounded windows in `user_availability`:
 
-**Repository role:** backend API only.
-
-**Jira title:** List team members of crm tl  
-**Story scope note:** The title emphasizes Part 3 (team listing), but the story description defines all three API parts below as in scope.
-
-## Goal
-
-Give CRM TLs secured APIs to manage and inspect availability of CRM users on their team, with server-side validation for role, team membership, datetime windows, and immutable past records.
+| Column | Purpose |
+|--------|---------|
+| `id` | Primary key |
+| `user_id` | CRM user |
+| `unavailable_from` | Start of unavailability (immutable after creation) |
+| `unavailable_to` | End of unavailability |
+| `cancelled_at` | Nullable timestamp when a future window was soft-cancelled |
+| `cancelled_by` | Nullable user id of the TL who soft-cancelled the window |
+| `created_by` | Actor who created the window |
+| `created_at`, `updated_at` | Timestamps |
 
-## Business Rules
+**Available** is implicit: a user is available when there is no non-cancelled active or future unavailability window.
 
-### Roles & access
+CRM TLs manage team member availability via:
 
-1. Only users with role **`CRM_TL`** may:
-   - Mark CRM user availability / unavailability
-   - Fetch team availability
-2. A `CRM_TL` may only manage users belonging to **their own team** (direct reports).
-3. `CRM_TL` identity is derived from the authenticated user (`@User()` decorator).
-4. Target users must be **CRM** role users on the TL's team.
-5. **Backend-only** — no frontend dependency and no cron job dependency.
+- `POST /api/users/availability/unavailable` — schedule or start unavailability
+- `POST /api/users/availability/available` — mark a user available (**this story**)
+- `GET /api/users/team/availability` — team availability dashboard
 
-### Availability windows
+**Current behavior:** `UserAvailabilityService.markAvailable` resolves only an **active** window (`unavailable_from <= now <= unavailable_to`, `cancelled_at IS NULL`) and sets `unavailable_to = now`. If no active window exists, it throws `BadRequestException` with message `No active unavailability window found`, even when the user has an upcoming window (`unavailable_from > now`).
 
-6. **Partial-day** unavailability is supported via datetime ranges (e.g., 15 Jun 11:00 → 15 Jun 16:00).
-7. **Multiple** unavailability records per user are allowed.
-8. **Past records must not be modified** after the window has ended.
-9. A user is **UNAVAILABLE** when `NOW` is between `unavailable_from` and `unavailable_to`; otherwise **AVAILABLE**.
-10. When multiple overlapping windows exist, the **latest active window wins** for current status display.
+## Goals
 
-## In Scope
+1. Preserve existing behavior for **active** unavailability windows (end by setting `unavailable_to = now`).
+2. Allow **soft cancellation** of **upcoming** unavailability windows via the same `markAvailable` endpoint.
+3. Reject requests when the user is already fully available with a clear error message.
+4. Retain all availability records for audit integrity (no deletes, no `unavailable_from` changes, no mutation of past windows).
 
-| Part | API | Deliverable |
-|------|-----|-------------|
-| 1 | `POST /api/users/availability/unavailable` | Create unavailability window |
-| 2 (optional) | `POST /api/users/availability/available` | End current active unavailability |
-| 3 | `GET /api/users/team/availability` | List TL team members with availability + CRM context |
+## Non-Goals
 
-| Area | Deliverable |
-|------|-------------|
-| Entity | `UserAvailability` mapping to `user_availability` |
-| DTOs | `MarkUnavailableDto`, `MarkAvailableDto` (if Part 2 implemented), `TeamAvailabilityDto` (response shape) |
-| Service | `UserAvailabilityService` with QueryBuilder logic |
-| Access control | TL team validation (shared helper on Users service) |
-| Controller | Secured endpoints on `user.controller.ts` |
-| Module wiring | Register entity/service in Users module |
+- Changing `markUnavailable` / unavailability creation logic or overlap validation.
+- Changing team availability listing or other availability read APIs (`getTeamAvailability`, etc.).
+- Adding cron jobs or background schedulers.
+- Refactoring modules or services beyond `markAvailable` and its resolver helpers.
+- UI work (backend-only repository).
 
-## Out of Scope
+## Definitions
 
-- Changes to the **IOM module** (read `ioms` / `iom_assignment_state` for counts only; do not modify IOM assignment logic).
-- Frontend UI.
-- Cron jobs or auto-expiry of availability windows.
-- Hardcoded project or user IDs.
+| Term | Definition |
+|------|------------|
+| **Now** | Server time at request handling (`new Date()`), consistent with existing service logic. |
+| **Non-cancelled window** | `cancelled_at IS NULL` |
+| **Active window** | Non-cancelled AND `unavailable_from <= now AND unavailable_to >= now` |
+| **Upcoming window** | Non-cancelled AND `unavailable_from > now AND unavailable_to >= now` |
+| **Relevant window** | Non-cancelled AND `unavailable_to >= now` (active or upcoming) |
+| **Past completed window** | `unavailable_to < now` (never mutated by this flow) |
+| **Already available** | No relevant window exists for the target user |
 
-## Database
+## Requirements
 
-### Existing tables (do not recreate)
+### Functional
 
-| Table | Usage |
-|-------|-------|
-| `users` | TL identity, team membership, user profile fields |
-| `user_availability` | Unavailability windows |
-| `ioms` | IOM assignment data |
-| `iom_assignment_state` | Current IOM allocation state |
+**R1 — Authorization (unchanged)**  
+`markAvailable` must continue to call `userService.validateTLAccess(loggedInUser.dbId, dto.userId)` before any availability mutation. Existing `ForbiddenException` behavior must be preserved.
 
-### `user_availability` (extend if needed)
+**R2 — Active window (unchanged semantics, stricter guard)**  
+If the target user has an **active** non-cancelled window (`unavailable_from <= now <= unavailable_to AND cancelled_at IS NULL`), set `unavailable_to = now` on that window, persist, and return the updated `UserAvailability` record. **Do not** modify `unavailable_from`, `cancelled_at`, or `cancelled_by`.
 
-| Column | Notes |
-|--------|-------|
-| `id` | PK |
-| `user_id` | FK → target CRM user |
-| `unavailable_from` | Window start |
-| `unavailable_to` | Window end |
-| `marked_by` | CRM TL user id who created/updated |
-| `reason` | Nullable free text |
-| `created_at` | |
-| `updated_at` | |
+**R3 — Upcoming window (soft-cancel)**  
+If there is **no** active window but there **is** an upcoming non-cancelled window (`unavailable_from > now AND cancelled_at IS NULL`), soft-cancel it by updating the row:
 
-Do not add a migration unless schema gaps are discovered during implementation.
+- `cancelled_at = now`
+- `cancelled_by = loggedInUser.dbId` (the authenticated CRM_TL)
 
-## API
+**Do not** modify `unavailable_from` or `unavailable_to`. **Do not delete** the row.
 
-**Conventions (from context map):**
+Persist and return the updated `UserAvailability` record.
 
-- Global prefix: `api` (non-prod may use `api/{NODE_ENV}`)
-- Style: REST
-- Response envelope: `success-response-errors`
+**R4 — Already available**  
+If there is neither an active nor an upcoming relevant window (only past completed windows exist, or no windows exist), throw `BadRequestException("User is already available")`. Do not mutate availability data.
 
-**Auth pattern:**
+**R5 — Resolution order**  
+Evaluate in this order:
 
-- `@User()` for authenticated user
-- `Guards` + `@Roles(CRM_TL)` for role enforcement
+1. Active window → apply R2  
+2. Else upcoming window → apply R3  
+3. Else → apply R4  
 
----
+**R6 — Window selection**  
+Introduce a resolver (e.g. `resolveRelevantWindow`) that fetches the earliest non-cancelled window where `unavailable_to >= now`:
 
-### Part 1: `POST /api/users/availability/unavailable`
+```sql
+WHERE user_id = :userId
+  AND cancelled_at IS NULL
+  AND unavailable_to >= :now
+ORDER BY unavailable_from ASC
+LIMIT 1
+```
 
-Mark a team CRM user unavailable for a datetime range.
+Then classify the result in service logic:
 
-**Role:** `CRM_TL` only
+- If `unavailable_from <= now` → treat as **active** (R2)
+- If `unavailable_from > now` → treat as **upcoming** (R3)
 
-**Request body (`MarkUnavailableDto`):**
+When multiple upcoming windows exist, only the **earliest** (`unavailable_from` minimum) is affected per call.
 
-| Field | Type | Required | Rules |
-|-------|------|----------|-------|
-| `userId` | number | yes | Target CRM user |
-| `unavailableFrom` | ISO datetime | yes | `>= now`; `< unavailableTo` |
-| `unavailableTo` | ISO datetime | yes | `> unavailableFrom` |
-| `reason` | string | no | Optional |
+**R7 — Active takes precedence**  
+If a user has both an active window and separate upcoming window(s), only the active window is ended (R2). Upcoming windows remain untouched until a subsequent call (if still relevant).
 
-**Behavior:**
+**R8 — Immutable constraints**  
+Across all branches:
 
-1. Validate `CRM_TL` access and target user is on TL's team.
-2. Validate datetime window (`from >= now`, `from < to`).
-3. Insert `user_availability` record with `marked_by =` authenticated TL.
-4. Return success response.
+- **NEVER** update `unavailable_from`
+- **NEVER** delete records
+- **NEVER** touch past completed windows (`unavailable_to < now`)
+- **NEVER** modify windows where `cancelled_at IS NOT NULL`
 
-**Errors:**
+**R9 — Endpoint contract (unchanged)**  
+Keep `POST /api/users/availability/available` with body `{ userId: number }` (`MarkAvailableDto`). Guards and role requirements remain: `RmAdminAuthGuard`, `RolesGuard`, `RolesEnum.CRM_TL`. Response envelope follows existing `success-response-errors` REST style with global prefix `api`.
 
-| Condition | Exception |
-|-----------|-----------|
-| TL not authorized, target not on team, target not CRM | `ForbiddenException` |
-| Invalid datetime window | `BadRequestException` |
+**R10 — Scope isolation**  
+All new logic must live inside `UserAvailabilityService`. No breaking changes to other modules. Do not alter unavailability creation or listing APIs.
 
----
+### Schema / Entity
 
-### Part 2 (optional): `POST /api/users/availability/available`
+**R11 — Soft-cancel columns**  
+The `user_availability` table must expose `cancelled_at` (nullable timestamp) and `cancelled_by` (nullable user id). If these columns are not yet present on the entity or in the database, add them via TypeORM entity update and migration as a prerequisite within this story. Listing and creation APIs must not be refactored; only ensure the entity supports persistence of soft-cancel fields used by `markAvailable`.
 
-End the user's **current active** unavailability window.
+### Non-Functional
 
-**Role:** `CRM_TL` only
+- Add/update unit tests in `user-availability.service.spec.ts` for all new branches.
+- Follow existing NestJS / TypeORM patterns in `UserAvailabilityService`.
+- Add clear inline comments in `markAvailable` explaining the decision flow.
+- No breaking changes to existing API contracts.
 
-**Request body:** Target `userId` (and any fields needed for validation).
+## Acceptance Criteria
 
-**Behavior:**
+1. **Active window unchanged**  
+   Given a user with an active non-cancelled unavailability window, when a CRM_TL calls `markAvailable`, then `unavailable_to` is set to approximately `now`, `unavailable_from` is unchanged, `cancelled_at` and `cancelled_by` remain null, the row is retained, and the updated record is returned.
 
-1. Validate TL access and team membership (same rules as Part 1).
-2. Find the active window where `now` is between `unavailable_from` and `unavailable_to`.
-3. Set `unavailable_to = now` on that record.
-4. Only affect **active/future** windows; **past records remain immutable**.
+2. **Upcoming window soft-cancelled**  
+   Given a user with no active window but an upcoming non-cancelled scheduled window, when a CRM_TL calls `markAvailable`, then that window is updated with `cancelled_at ≈ now` and `cancelled_by = loggedIn TL dbId`, `unavailable_from` and `unavailable_to` are unchanged, the row is retained (not deleted), and the updated record is returned.
 
-**Errors:**
+3. **Already available rejected**  
+   Given a user with no active and no upcoming relevant windows (only past windows or no records), when a CRM_TL calls `markAvailable`, then the API responds with `400 Bad Request` and message `User is already available`, and no availability data is mutated.
 
-| Condition | Exception |
-|-----------|-----------|
-| Unauthorized / not on team | `ForbiddenException` |
-| No active unavailability window | `BadRequestException` (or appropriate not-found semantics) |
+4. **Active window takes precedence**  
+   Given a user with both an active window and a separate upcoming window, when a CRM_TL calls `markAvailable`, then only the active window is ended; the upcoming window is left untouched.
 
----
+5. **Earliest upcoming window**  
+   Given multiple upcoming non-cancelled windows and no active window, when a CRM_TL calls `markAvailable`, then only the earliest upcoming window (`unavailable_from` minimum) is soft-cancelled.
 
-### Part 3: `GET /api/users/team/availability`
+6. **Past windows untouched**  
+   Given a user with only past completed windows (`unavailable_to < now`), when a CRM_TL calls `markAvailable`, then the API returns `400 User is already available` and no historical rows are modified.
 
-List all CRM users on the authenticated TL's team with computed availability and CRM context.
+7. **Already-cancelled windows ignored**  
+   Given a user whose only future-dated rows have `cancelled_at` set, when a CRM_TL calls `markAvailable`, then the API returns `400 User is already available`.
 
-**Role:** `CRM_TL` only
+8. **Authorization preserved**  
+   Given a TL without access to the target user, `markAvailable` returns `403 Forbidden` without changing availability.
 
-**Response — for each team CRM user:**
+9. **Scope limited**  
+   `markUnavailable`, listing/read APIs, cron jobs, and unrelated modules are unchanged.
 
-| Field | Type | Notes |
-|-------|------|-------|
-| `userId` | number | |
-| `empId` | string | |
-| `name` | string | |
-| `email` | string | |
-| `role` | string | Expected value: `CRM` |
-| `currentStatus` | `AVAILABLE` \| `UNAVAILABLE` | Computed at request time |
-| `unavailableFrom` | ISO datetime | Present only when `UNAVAILABLE` |
-| `unavailableTo` | ISO datetime | Present only when `UNAVAILABLE` |
-| `projects` | `{ id, name }[]` | Assigned projects |
-| `allocatedIomsCount` | number | Currently assigned IOMs |
-| `pendingIomsCount` | number | Optional, include if straightforward |
+10. **Tests**  
+    Unit tests cover: active window end, upcoming soft-cancel, already-available error, active-over-upcoming precedence, earliest-upcoming selection, past-window no-op, cancelled-window exclusion, and no-delete audit behavior.
 
-**Status logic:**
+## UI Notes
 
-```
-UNAVAILABLE if NOW BETWEEN unavailable_from AND unavailable_to
-otherwise AVAILABLE
-```
+No UI changes in this repository. Clients calling `POST /api/users/availability/available` should handle success when soft-cancelling an upcoming window and the clarified `User is already available` error when no relevant window exists.
 
-When multiple windows overlap, use the **latest active** window for `unavailableFrom` / `unavailableTo`.
-
-**Example response:**
-
-```json
-{
-  "data": [
-    {
-      "userId": 101,
-      "empId": "CRM1021",
-      "name": "Amit Sharma",
-      "email": "amit@company.com",
-      "role": "CRM",
-      "currentStatus": "UNAVAILABLE",
-      "unavailableFrom": "2026-06-15T11:00:00Z",
-      "unavailableTo": "2026-06-15T16:00:00Z",
-      "projects": [
-        { "id": 58, "name": "Project Alpha" }
-      ],
-      "allocatedIomsCount": 12
-    }
-  ]
-}
-```
+## Implementation Notes
 
-## Edge Cases
+### Primary touchpoints
 
-| Case | Expected behavior |
-|------|-------------------|
-| User unavailable now but available later today | `currentStatus = UNAVAILABLE` with today's window; becomes `AVAILABLE` after `unavailable_to` |
-| Multiple future unavailability windows | Allowed; GET shows current active window only |
-| No availability records | `currentStatus = AVAILABLE`; omit `unavailableFrom` / `unavailableTo` |
-| Zero IOMs assigned | `allocatedIomsCount = 0` |
-| Overlapping windows | Latest active window wins for status display |
-| Past windows | Never modified by mark-available or new inserts |
+| File | Change |
+|------|--------|
+| `src/modules/users/services/user-availability.service.ts` | Extend `markAvailable`; add resolver helper (e.g. `resolveRelevantWindow`) |
+| `src/modules/users/services/user-availability.service.spec.ts` | New/updated test cases for R2–R7, R10 |
+| `src/modules/users/entities/user-availability.entity.ts` | Add `cancelledAt`, `cancelledBy` if not present |
+| `src/migrations/*` | Add migration for `cancelled_at`, `cancelled_by` if columns missing |
+| `src/modules/users/user.controller.ts` | No change expected |
+| `src/modules/users/dto/mark-available.dto.ts` | No change expected |
 
-## Implementation Notes
-
-### Module structure
+### Suggested logic
 
 ```
-src/modules/users/
-├── user.controller.ts
-├── services/
-│   └── user-availability.service.ts
-├── entities/
-│   └── user-availability.entity.ts
-└── dto/
-    ├── mark-unavailable.dto.ts
-    └── team-availability.dto.ts
+markAvailable(loggedInUser, dto):
+  validateTLAccess(loggedInUser.dbId, dto.userId)
+
+  now = new Date()
+  window = resolveRelevantWindow(dto.userId, now)
+    // WHERE user_id = :userId
+    //   AND cancelled_at IS NULL
+    //   AND unavailable_to >= :now
+    // ORDER BY unavailable_from ASC LIMIT 1
+
+  if not window:
+    throw BadRequestException('User is already available')
+
+  if window.unavailableFrom <= now:
+    // Active — end window early
+    window.unavailableTo = now
+  else:
+    // Upcoming — soft cancel (preserve original from/to)
+    window.cancelledAt = now
+    window.cancelledBy = loggedInUser.dbId
+
+  return save(window)
 ```
 
-### Technical requirements
-
-- Use **TypeORM QueryBuilder** for team listing, availability lookup, and IOM/project aggregation.
-- Inject repositories directly; avoid circular imports between Users and IOM modules.
-- Reuse existing **Users** entity for team membership and profile fields.
-- Throw `BadRequestException` / `ForbiddenException` with clear messages.
-- Do **not** modify the IOM module.
-
-### Suggested service methods
-
-| Method | Purpose |
-|--------|---------|
-| `markUnavailable(loggedInUser, dto)` | Part 1 |
-| `markAvailable(loggedInUser, dto)` | Part 2 (if implemented) |
-| `getTeamAvailability(loggedInUser)` | Part 3 |
-
-### Team membership validation
-
-Shared helper (e.g., `validateTLAccess(tlUserId, targetUserId)`):
-
-1. TL cannot act on themselves (unless product says otherwise — default: forbid).
-2. Target user must exist and have CRM role.
-3. Target user must belong to TL's team (direct report relationship).
-
-### CRM context queries
-
-For Part 3, join or subquery against `ioms` and `iom_assignment_state` to compute:
-
-- `projects` — distinct assigned projects (`id`, `name`)
-- `allocatedIomsCount` — count of currently assigned IOMs
-- `pendingIomsCount` — optional if simple to derive
-
-### Deliverables for implementer
-
-1. Controller methods
-2. Service methods
-3. DTOs
-4. QueryBuilder logic
-5. Helper functions (TL access, active window resolution)
-6. Sample `curl` commands for each endpoint
-
-## Acceptance Criteria
-
-### Part 1 — Mark unavailable
+### Resolver helper
+
+```typescript
+private async resolveRelevantWindow(
+  userId: number,
+  now: Date,
+): Promise<UserAvailability | null> {
+  return this.availabilityRepo
+    .createQueryBuilder('ua')
+    .where('ua.user_id = :userId', { userId })
+    .andWhere('ua.cancelled_at IS NULL')
+    .andWhere('ua.unavailable_to >= :now', { now })
+    .orderBy('ua.unavailable_from', 'ASC')
+    .getOne();
+}
+```
 
-1. `POST /api/users/availability/unavailable` requires authentication and `CRM_TL` role.
-2. CRM TL can create an unavailability window for a CRM direct report with `unavailableFrom >= now` and `unavailableFrom < unavailableTo`.
-3. Partial-day windows (same calendar day, different times) persist correctly.
-4. Record stores `user_id`, `unavailable_from`, `unavailable_to`, `marked_by` (authenticated TL), and optional `reason`.
-5. CRM TL receives `ForbiddenException` when target is not on their team or is not a CRM user.
-6. Invalid datetime windows return `BadRequestException`.
-7. Past unavailability records are never updated by this endpoint.
+Classification of active vs upcoming remains in `markAvailable` service logic, not in the resolver.
 
-### Part 2 — Mark available (if implemented)
+### Deliverables
 
-8. `POST /api/users/availability/available` ends the **current active** window by setting `unavailable_to = now`.
-9. Only active/future windows are affected; completed past windows remain unchanged.
-10. Unauthorized or non-team targets return `ForbiddenException`.
+1. Updated `markAvailable()` method implementing R2–R5 with soft-cancel for upcoming windows.
+2. New resolver method (e.g. `resolveRelevantWindow`) implementing R6.
+3. Entity/migration for `cancelled_at` and `cancelled_by` if not already present.
+4. Clear inline comments explaining the decision flow.
+5. Unit tests for all acceptance criteria.
 
-### Part 3 — List team availability
+### Return value
 
-11. `GET /api/users/team/availability` requires `CRM_TL` role and returns only the authenticated TL's team members.
-12. Each team member includes `userId`, `empId`, `name`, `email`, and `role`.
-13. `currentStatus` is computed in real time: `UNAVAILABLE` when `now` is within an active window, otherwise `AVAILABLE`.
-14. When `UNAVAILABLE`, response includes `unavailableFrom` and `unavailableTo` for the active window.
-15. When `AVAILABLE`, `unavailableFrom` and `unavailableTo` are omitted (or null per existing API convention).
-16. Response includes `projects` (id + name) and `allocatedIomsCount` per user.
-17. Users with no availability records appear as `AVAILABLE` with zero IOMs if applicable.
-18. Overlapping windows resolve to the latest active window for display.
+Method signature remains `Promise<UserAvailability>`. Return the persisted entity after update in all success paths (active end and upcoming soft-cancel).
 
-### Cross-cutting
+### Supersedes prior approach
 
-19. All availability logic lives under `src/modules/users/`; IOM module is not modified.
-20. No frontend or cron dependencies are introduced.
-21. APIs use the repository's standard response envelope and global `api` prefix.
+Earlier drafts collapsed upcoming windows by setting `unavailable_from = now` and `unavailable_to = now`. That approach is **replaced** by soft-cancel via `cancelled_at` / `cancelled_by` to preserve original scheduled dates for historical accuracy.
 
 ## Assumptions
 
-- `user_availability`, `users`, `ioms`, and `iom_assignment_state` tables already exist with usable schemas.
-- `CRM_TL` and `CRM` role constants and `@Roles()` guard patterns already exist in the codebase.
-- Team hierarchy is expressed via a direct-report field on `Users` (e.g., `reportingTo` / `reporting_to`); implementation planner should confirm the exact column name.
-- Part 2 is labeled **optional** in the story but should be implemented unless explicitly descoped during planning.
-- `pendingIomsCount` is included only if derivable with simple queries; otherwise omit without blocking delivery.
-- ISO datetimes are stored and compared consistently (prefer UTC alignment with existing datetime conventions).
+1. **Soft-cancel strategy:** Upcoming windows are cancelled by setting `cancelled_at` and `cancelled_by` only; original `unavailable_from` and `unavailable_to` values are preserved for audit.
+2. **Error message:** Already-available case uses exactly `User is already available` (replacing the prior `No active unavailability window found` for that branch).
+3. **Multiple upcoming windows:** Only the earliest non-cancelled upcoming window is soft-cancelled per `markAvailable` call; additional windows require subsequent calls.
+4. **Resolver naming:** `resolveRelevantWindow` (or equivalent) is the expected helper; naming may vary if query semantics in R6 are preserved.
+5. **Listing APIs out of scope:** Per story constraints, listing/read APIs are not modified in this story. If they currently ignore `cancelled_at`, a follow-up may be needed for dashboard accuracy; that is not part of PN-39.
+6. **Column existence:** Change request assumes `cancelled_at` and `cancelled_by` exist or will be added; implementation should verify entity/DB state and add migration only if missing.
 
 ## Open Questions
 
-1. **Title vs scope:** Jira title is "List team members of crm tl" but the description includes Parts 1–3. Confirm whether Part 1 (already implemented in a prior pass) should be re-validated or only Parts 2–3 are net-new for this execution.
-2. **Overlap on insert:** Edge cases say "latest active wins" for display, but Part 1 does not explicitly forbid overlapping inserts. Confirm whether overlapping windows should be rejected on create or allowed.
-3. **Team field name:** Story says "belonging to their own team" without naming the FK column; confirm `reporting_to` vs `managerId` (or equivalent) during implementation planning.
-4. **Auth guard naming:** Story references generic `Guards`; confirm whether `RmAdminAuthGuard` + `RolesGuard` is the standard for Users endpoints in this repo.
-5. **Entity location:** If a `UserAvailability` entity already exists under IOM or `src/entities/`, prefer relocating/reusing a single definition rather than duplicating mappings.
-6. **Mark-available request shape:** Exact DTO fields for Part 2 are not fully specified beyond `userId`; confirm during planning.
+1. **Listing behavior with soft-cancelled windows:** If `getTeamAvailability` does not yet filter `cancelled_at IS NULL`, users with soft-cancelled future windows may still appear unavailable on the dashboard until a separate change is made. Confirm whether that is acceptable for this release or requires a coordinated follow-up (out of PN-39 scope per constraints).
 
 ## References
 
 - Story key: **PN-39**
-- Context map: `docs/ai/context-map.json` — API prefix `api`, REST, `success-response-errors` envelope
-- Target spec path: `docs/ai/stories/PN-39/spec.md`
-- Related tables: `users`, `user_availability`, `ioms`, `iom_assignment_state`
+- Service: `src/modules/users/services/user-availability.service.ts` (`markAvailable`, `resolveActiveWindow`)
+- Controller: `src/modules/users/user.controller.ts` (`POST availability/available`)
+- Entity: `src/modules/users/entities/user-availability.entity.ts`
+- Tests: `src/modules/users/services/user-availability.service.spec.ts` (`describe('markAvailable')`)
+- API conventions: REST, global prefix `api`, `success-response-errors` envelope (from context map)
+- Active change request (2026-06-16): soft-cancel logic with `cancelled_at` / `cancelled_by` supersedes prior collapse-based cancellation
diff --git a/src/modules/users/entities/user-availability.entity.ts b/src/modules/users/entities/user-availability.entity.ts
index 4cffad9a..097b499b 100644
--- a/src/modules/users/entities/user-availability.entity.ts
+++ b/src/modules/users/entities/user-availability.entity.ts
@@ -44,6 +44,12 @@ export class UserAvailability {
   @Column({ name: 'reason', type: 'varchar', length: 255, nullable: true })
   reason: string | null;
 
+  @Column({ name: 'cancelled_at', type: 'datetime', nullable: true })
+  cancelledAt: Date | null;
+
+  @Column({ name: 'cancelled_by', type: 'int', nullable: true })
+  cancelledBy: number | null;
+
   @CreateDateColumn({ name: 'created_at', type: 'timestamp' })
   createdAt: Date;
 
diff --git a/src/modules/users/services/user-availability.service.spec.ts b/src/modules/users/services/user-availability.service.spec.ts
index feb1b329..e0b18972 100644
--- a/src/modules/users/services/user-availability.service.spec.ts
+++ b/src/modules/users/services/user-availability.service.spec.ts
@@ -313,15 +313,199 @@ describe('UserAvailabilityService', () => {
           unavailableTo: expect.any(Date),
         }),
       );
+      expect(availabilityRepo.update).toHaveBeenCalledWith(
+        activeWindow.id,
+        expect.not.objectContaining({
+          cancelledAt: expect.anything(),
+          cancelledBy: expect.anything(),
+          unavailableFrom: expect.anything(),
+        }),
+      );
       expect(result.id).toBe(activeWindow.id);
     });
 
-    it('throws BadRequestException when no active window exists', async () => {
+    it('throws BadRequestException when user is already available', async () => {
       availabilityQb.getOne.mockResolvedValueOnce(null);
 
       await expect(
         service.markAvailable(tlUser, { userId: targetUserId }),
-      ).rejects.toThrow(BadRequestException);
+      ).rejects.toThrow(new BadRequestException('User is already available'));
+
+      expect(availabilityRepo.update).not.toHaveBeenCalled();
+    });
+
+    it('soft-cancels an upcoming window without modifying window dates', async () => {
+      const upcomingWindow = {
+        id: 7,
+        userId: targetUserId,
+        unavailableFrom: new Date(Date.now() + 60 * 60 * 1000),
+        unavailableTo: new Date(Date.now() + 2 * 60 * 60 * 1000),
+      };
+      const softCancelledWindow = {
+        ...upcomingWindow,
+        cancelledAt: new Date(),
+        cancelledBy: tlUser.dbId,
+      };
+
+      availabilityQb.getOne.mockResolvedValueOnce(upcomingWindow);
+      availabilityRepo.findOne.mockResolvedValueOnce(softCancelledWindow);
+
+      const before = Date.now();
+      const result = await service.markAvailable(tlUser, {
+        userId: targetUserId,
+      });
+      const after = Date.now();
+
+      expect(availabilityRepo.update).toHaveBeenCalledWith(
+        upcomingWindow.id,
+        expect.objectContaining({
+          cancelledAt: expect.any(Date),
+          cancelledBy: tlUser.dbId,
+        }),
+      );
+      const updatePayload = availabilityRepo.update.mock.calls[0][1];
+      expect(updatePayload.cancelledAt.getTime()).toBeGreaterThanOrEqual(
+        before,
+      );
+      expect(updatePayload.cancelledAt.getTime()).toBeLessThanOrEqual(after);
+      expect(updatePayload).not.toHaveProperty('unavailableFrom');
+      expect(updatePayload).not.toHaveProperty('unavailableTo');
+      expect(availabilityRepo.save).not.toHaveBeenCalled();
+      expect(result.id).toBe(upcomingWindow.id);
+      expect(result.unavailableFrom).toEqual(upcomingWindow.unavailableFrom);
+      expect(result.unavailableTo).toEqual(upcomingWindow.unavailableTo);
+    });
+
+    it('ends an active window without modifying unavailableFrom', async () => {
+      const activeWindow = {
+        id: 8,
+        userId: targetUserId,
+        unavailableFrom: new Date(Date.now() - 60 * 60 * 1000),
+        unavailableTo: new Date(Date.now() + 60 * 60 * 1000),
+      };
+
+      availabilityQb.getOne.mockResolvedValueOnce(activeWindow);
+      availabilityRepo.findOne.mockResolvedValueOnce({
+        ...activeWindow,
+        unavailableTo: new Date(),
+      });
+
+      await service.markAvailable(tlUser, { userId: targetUserId });
+
+      expect(availabilityRepo.update).toHaveBeenCalledWith(
+        activeWindow.id,
+        expect.objectContaining({
+          unavailableTo: expect.any(Date),
+        }),
+      );
+      expect(availabilityRepo.update).toHaveBeenCalledWith(
+        activeWindow.id,
+        expect.not.objectContaining({
+          unavailableFrom: expect.anything(),
+        }),
+      );
+    });
+
+    it('soft-cancels only the earliest upcoming window when multiple exist', async () => {
+      const earliestUpcoming = {
+        id: 9,
+        userId: targetUserId,
+        unavailableFrom: new Date(Date.now() + 30 * 60 * 1000),
+        unavailableTo: new Date(Date.now() + 90 * 60 * 1000),
+      };
+
+      availabilityQb.getOne.mockResolvedValueOnce(earliestUpcoming);
+      availabilityRepo.findOne.mockResolvedValueOnce({
+        ...earliestUpcoming,
+        cancelledAt: new Date(),
+        cancelledBy: tlUser.dbId,
+      });
+
+      await service.markAvailable(tlUser, { userId: targetUserId });
+
+      expect(availabilityRepo.update).toHaveBeenCalledTimes(1);
+      expect(availabilityRepo.update).toHaveBeenCalledWith(
+        earliestUpcoming.id,
+        expect.objectContaining({
+          cancelledAt: expect.any(Date),
+          cancelledBy: tlUser.dbId,
+        }),
+      );
+    });
+
+    it('does not delete rows when soft-cancelling an upcoming window', async () => {
+      const upcomingWindow = {
+        id: 10,
+        userId: targetUserId,
+        unavailableFrom: new Date(Date.now() + 60 * 60 * 1000),
+        unavailableTo: new Date(Date.now() + 2 * 60 * 60 * 1000),
+      };
+
+      availabilityQb.getOne.mockResolvedValueOnce(upcomingWindow);
+      availabilityRepo.findOne.mockResolvedValueOnce({
+        ...upcomingWindow,
+        cancelledAt: new Date(),
+        cancelledBy: tlUser.dbId,
+      });
+
+      await service.markAvailable(tlUser, { userId: targetUserId });
+
+      expect(availabilityRepo.update).toHaveBeenCalledTimes(1);
+      expect(availabilityRepo.findOne).toHaveBeenCalledTimes(1);
+      expect(availabilityRepo.save).not.toHaveBeenCalled();
+    });
+
+    it('filters cancelled windows via resolver', async () => {
+      availabilityQb.getOne.mockResolvedValueOnce(null);
+
+      await expect(
+        service.markAvailable(tlUser, { userId: targetUserId }),
+      ).rejects.toThrow(new BadRequestException('User is already available'));
+
+      expect(availabilityQb.andWhere).toHaveBeenCalledWith(
+        'ua.cancelled_at IS NULL',
+      );
+      expect(availabilityRepo.update).not.toHaveBeenCalled();
+    });
+
+    it('throws when only past windows exist', async () => {
+      availabilityQb.getOne.mockResolvedValueOnce(null);
+
+      await expect(
+        service.markAvailable(tlUser, { userId: targetUserId }),
+      ).rejects.toThrow(new BadRequestException('User is already available'));
+
+      expect(availabilityRepo.update).not.toHaveBeenCalled();
+    });
+
+    it('ends active window when both active and upcoming windows exist', async () => {
+      const activeWindow = {
+        id: 11,
+        userId: targetUserId,
+        unavailableFrom: new Date(Date.now() - 60 * 60 * 1000),
+        unavailableTo: new Date(Date.now() + 60 * 60 * 1000),
+      };
+
+      availabilityQb.getOne.mockResolvedValueOnce(activeWindow);
+      availabilityRepo.findOne.mockResolvedValueOnce({
+        ...activeWindow,
+        unavailableTo: new Date(),
+      });
+
+      await service.markAvailable(tlUser, { userId: targetUserId });
+
+      expect(availabilityRepo.update).toHaveBeenCalledWith(
+        activeWindow.id,
+        expect.objectContaining({
+          unavailableTo: expect.any(Date),
+        }),
+      );
+      expect(availabilityRepo.update).not.toHaveBeenCalledWith(
+        expect.anything(),
+        expect.objectContaining({
+          cancelledAt: expect.anything(),
+        }),
+      );
     });
 
     it('throws ForbiddenException when TL access is denied', async () => {
diff --git a/src/modules/users/services/user-availability.service.ts b/src/modules/users/services/user-availability.service.ts
index d1ce2011..c6e46100 100644
--- a/src/modules/users/services/user-availability.service.ts
+++ b/src/modules/users/services/user-availability.service.ts
@@ -90,17 +90,26 @@ export class UserAvailabilityService {
     await this.userService.validateTLAccess(loggedInUser.dbId, dto.userId);
 
     const now = new Date();
-    const activeWindow = await this.resolveActiveWindow(dto.userId, now);
+    const window = await this.resolveRelevantWindow(dto.userId, now);
 
-    if (!activeWindow) {
-      throw new BadRequestException('No active unavailability window found');
+    if (!window) {
+      throw new BadRequestException('User is already available');
     }
 
-    await this.availabilityRepo.update(activeWindow.id, {
-      unavailableTo: now,
-    });
+    if (window.unavailableFrom <= now) {
+      // Active window: end early — preserve unavailable_from and cancel fields
+      await this.availabilityRepo.update(window.id, {
+        unavailableTo: now,
+      });
+    } else {
+      // Upcoming window: soft-cancel — preserve original window dates
+      await this.availabilityRepo.update(window.id, {
+        cancelledAt: now,
+        cancelledBy: loggedInUser.dbId,
+      });
+    }
 
-    return this.availabilityRepo.findOne({ where: { id: activeWindow.id } });
+    return this.availabilityRepo.findOne({ where: { id: window.id } });
   }
 
   async getTeamAvailability(loggedInUser: {
@@ -151,16 +160,16 @@ export class UserAvailabilityService {
     });
   }
 
-  private async resolveActiveWindow(
+  private async resolveRelevantWindow(
     userId: number,
     now: Date,
   ): Promise<UserAvailability | null> {
     return this.availabilityRepo
       .createQueryBuilder('ua')
       .where('ua.user_id = :userId', { userId })
-      .andWhere('ua.unavailable_from <= :now', { now })
+      .andWhere('ua.cancelled_at IS NULL')
       .andWhere('ua.unavailable_to >= :now', { now })
-      .orderBy('ua.unavailable_from', 'DESC')
+      .orderBy('ua.unavailable_from', 'ASC')
       .getOne();
   }

## Staged Diff

(No staged index changes.)
