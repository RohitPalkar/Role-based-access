# Working Tree Diff
# Includes unstaged and staged changes.
# Generated by iProgrammer Flow for execution exec-023ab224-44a6-4554-be14-d40997f3e336.

## Unstaged Diff
diff --git a/src/modules/iom/enums/iom-status-code.enum.ts b/src/modules/iom/enums/iom-status-code.enum.ts
index c4c9d9cd..07210d63 100644
--- a/src/modules/iom/enums/iom-status-code.enum.ts
+++ b/src/modules/iom/enums/iom-status-code.enum.ts
@@ -14,11 +14,18 @@ export enum IomStatusCodeEnum {
   TL_REJECTED = 'TL_REJECTED',
   CRM_HEAD_APPROVED = 'CRM_HEAD_APPROVED',
   CRM_HEAD_REJECTED = 'CRM_HEAD_REJECTED',
+  FINANCE_VERIFICATION_PENDING = 'FINANCE_VERIFICATION_PENDING',
   FINANCE_VERIFIED = 'FINANCE_VERIFIED',
-  FINANCE_APPROVED = 'FINANCE_APPROVED',
   FINANCE_REJECTED = 'FINANCE_REJECTED',
+  FINANCE_APPROVED = 'FINANCE_APPROVED',
   FINANCE_APPROVER_REJECTED = 'FINANCE_APPROVER_REJECTED',
+  POINTS_TO_BE_UPLOADED = 'POINTS_TO_BE_UPLOADED',
+  POINTS_UPLOADED = 'POINTS_UPLOADED',
   POINTS_ALLOTTED = 'POINTS_ALLOTTED',
+  INVOICE_REQUESTED = 'INVOICE_REQUESTED',
+  INVOICE_SUBMITTED = 'INVOICE_SUBMITTED',
+  INVOICE_RECEIVED = 'INVOICE_RECEIVED',
+  IOM_CLOSED = 'IOM_CLOSED',
   COMPLETED = 'COMPLETED',
   CANCELLED = 'CANCELLED',
 }
diff --git a/src/modules/iom/iom.controller.spec.ts b/src/modules/iom/iom.controller.spec.ts
index f08670a3..ab37a7c3 100644
--- a/src/modules/iom/iom.controller.spec.ts
+++ b/src/modules/iom/iom.controller.spec.ts
@@ -6,6 +6,11 @@ import { IomController } from './iom.controller';
 import { IomCrmService } from './services/iom-crm.service';
 import { IomEligibilityService } from './services/iom-eligibility.service';
 import { IomListingService } from './services/iom-listing.service';
+import { IomExportService } from './services/iom-export.service';
+import { IomRejectService } from './services/iom-reject.service';
+import { IomApproveService } from './services/iom-approve.service';
+import { IomCancelService } from './services/iom-cancel.service';
+import { IomAssignmentService } from './services/iom-assignment.service';
 import { RmAdminAuthGuard } from '../sso/gaurds/rm-admin-auth.gaurd';
 import { RolesGuard } from '../sso/gaurds/roles.gaurd';
 import { RolesEnum } from 'src/enums/roles.enum';
@@ -22,6 +27,7 @@ describe('IomController', () => {
   let controller: IomController;
   const eligibilityService = { findEligible: jest.fn() };
   const iomListingService = { findIoms: jest.fn() };
+  const iomExportService = { exportToExcel: jest.fn() };
   const crmService = {};
 
   beforeEach(async () => {
@@ -31,6 +37,11 @@ describe('IomController', () => {
         { provide: IomCrmService, useValue: crmService },
         { provide: IomEligibilityService, useValue: eligibilityService },
         { provide: IomListingService, useValue: iomListingService },
+        { provide: IomExportService, useValue: iomExportService },
+        { provide: IomRejectService, useValue: {} },
+        { provide: IomApproveService, useValue: {} },
+        { provide: IomCancelService, useValue: {} },
+        { provide: IomAssignmentService, useValue: {} },
         { provide: EventEmitter2, useValue: { emit: jest.fn() } },
       ],
     })
@@ -46,7 +57,7 @@ describe('IomController', () => {
   afterEach(() => jest.clearAllMocks());
 
   describe('list', () => {
-    it('delegates to IomListingService when listType is ioms', async () => {
+    it('delegates to IomListingService', async () => {
       const expected = {
         items: [],
         total: 0,
@@ -61,38 +72,29 @@ describe('IomController', () => {
       expect(iomListingService.findIoms).toHaveBeenCalledWith(CRM_USER, {
         listType: 'ioms',
       });
-      expect(eligibilityService.findEligible).not.toHaveBeenCalled();
-      expect(result).toBe(expected);
+      expect(result).toEqual({ data: expected });
     });
+  });
 
-    it('delegates to IomEligibilityService by default', async () => {
+  describe('exportExcel', () => {
+    it('delegates to IomExportService', async () => {
       const expected = {
-        items: [],
-        total: 0,
-        page: 1,
-        limit: 10,
-        totalPages: 0,
+        data: {
+          fileUrl: 'exports/iom-list-18Jun_2-30PM.xlsx',
+          baseUrl: 'https://cdn.example.com/puravankara/',
+        },
       };
-      eligibilityService.findEligible.mockResolvedValue(expected);
+      iomExportService.exportToExcel.mockResolvedValue(expected);
 
-      const result = await controller.list(CRM_USER, {});
+      const result = await controller.exportExcel(CRM_USER, {
+        fields: ['iomNo', 'projectName'],
+      });
 
-      expect(eligibilityService.findEligible).toHaveBeenCalledWith(
-        CRM_USER,
-        {},
-      );
-      expect(iomListingService.findIoms).not.toHaveBeenCalled();
+      expect(iomExportService.exportToExcel).toHaveBeenCalledWith(CRM_USER, {
+        fields: ['iomNo', 'projectName'],
+      });
       expect(result).toBe(expected);
     });
-
-    it('delegates to IomEligibilityService when listType is eligible', async () => {
-      eligibilityService.findEligible.mockResolvedValue({ items: [] });
-
-      await controller.list(CRM_USER, { listType: 'eligible' });
-
-      expect(eligibilityService.findEligible).toHaveBeenCalled();
-      expect(iomListingService.findIoms).not.toHaveBeenCalled();
-    });
   });
 
   describe('validation pipe settings', () => {
diff --git a/src/modules/iom/iom.controller.ts b/src/modules/iom/iom.controller.ts
index bdc17ec5..194c38bc 100644
--- a/src/modules/iom/iom.controller.ts
+++ b/src/modules/iom/iom.controller.ts
@@ -30,9 +30,11 @@ import { IomApproveService } from './services/iom-approve.service';
 import { IomCancelService } from './services/iom-cancel.service';
 import { IomListingService } from './services/iom-listing.service';
 import { IomAssignmentService } from './services/iom-assignment.service';
+import { IomExportService } from './services/iom-export.service';
 import { AuthenticatedUser } from './services/iom-validation.service';
 
 import { ListIomListingDto } from './dto/list-iom-listing.dto';
+import { ExportIomExcelDto } from './dto/export-iom-excel.dto';
 import { GenerateIomDto } from './dto/generate-iom.dto';
 import { EditIomDto } from './dto/edit-iom.dto';
 import { SubmitIomDto } from './dto/submit-iom.dto';
@@ -81,6 +83,7 @@ export class IomController {
     private readonly cancelService: IomCancelService,
     private readonly iomListingService: IomListingService,
     private readonly assignmentService: IomAssignmentService,
+    private readonly iomExportService: IomExportService,
   ) {}
 
   // ---- 1. Listing (eligible bookings or persisted IOMs) -----------------
@@ -103,6 +106,25 @@ export class IomController {
     return { data: iomList };
   }
 
+  // ---- 1b. Export IOM list to Excel ------------------------------------
+  @UseGuards(RmAdminAuthGuard, RolesGuard)
+  @Roles(
+    RolesEnum.CRM,
+    RolesEnum.CRM_TL,
+    RolesEnum.CRM_HEAD,
+    RolesEnum.FINANCE_USER,
+    RolesEnum.FINANCE_HEAD,
+    RolesEnum.LOYALTY,
+    RolesEnum.ADMIN,
+  )
+  @Post('export/excel')
+  async exportExcel(
+    @User() user: AuthenticatedUser,
+    @Body() dto: ExportIomExcelDto,
+  ) {
+    return this.iomExportService.exportToExcel(user, dto);
+  }
+
   // ---- 2. Assign eligible IOMs (round-robin) ----------------------------
   @UseGuards(RmAdminAuthGuard, RolesGuard)
   @Roles(RolesEnum.ADMIN)
diff --git a/src/modules/iom/iom.module.ts b/src/modules/iom/iom.module.ts
index ab43bc49..d3bd5213 100644
--- a/src/modules/iom/iom.module.ts
+++ b/src/modules/iom/iom.module.ts
@@ -1,6 +1,8 @@
 import { Module } from '@nestjs/common';
 import { TypeOrmModule } from '@nestjs/typeorm';
 
+import { AwsModule } from 'src/modules/aws/aws.module';
+
 import { Iom } from './entities/iom.entity';
 import { IomStatus } from './entities/iom-status.entity';
 import { IomTransition } from './entities/iom-transition.entity';
@@ -30,6 +32,7 @@ import { IomApprovalNotificationService } from './services/iom-approval-notifica
 import { IomApproveService } from './services/iom-approve.service';
 import { IomCancelService } from './services/iom-cancel.service';
 import { IomSubmissionNotificationService } from './services/iom-submission-notification.service';
+import { IomExportService } from './services/iom-export.service';
 
 /**
  * CRM-USER-scoped IOM module.
@@ -41,6 +44,7 @@ import { IomSubmissionNotificationService } from './services/iom-submission-noti
  */
 @Module({
   imports: [
+    AwsModule,
     TypeOrmModule.forFeature([
       Iom,
       IomStatus,
@@ -72,6 +76,7 @@ import { IomSubmissionNotificationService } from './services/iom-submission-noti
     IomApproveService,
     IomCancelService,
     IomSubmissionNotificationService,
+    IomExportService,
   ],
   exports: [
     WorkflowValidationService,
diff --git a/src/modules/iom/services/iom-listing.service.spec.ts b/src/modules/iom/services/iom-listing.service.spec.ts
index 49028d56..9de011e5 100644
--- a/src/modules/iom/services/iom-listing.service.spec.ts
+++ b/src/modules/iom/services/iom-listing.service.spec.ts
@@ -40,6 +40,7 @@ const makeIom = (overrides: Partial<Iom> = {}): Iom =>
     refereePoints: 50_000,
     referralPointsEdited: false,
     referralClassification: 'CLASS_A',
+    loyaltyPointClassification: 'CLASS_A',
     customerDetails: null,
     submittedAt: null,
     createdAt: new Date('2026-01-15T10:00:00Z'),
@@ -68,6 +69,7 @@ describe('IomListingService', () => {
       orderBy: jest.fn().mockReturnThis(),
       skip: jest.fn().mockReturnThis(),
       take: jest.fn().mockReturnThis(),
+      getMany: jest.fn().mockResolvedValue([makeIom()]),
       getManyAndCount: jest.fn().mockResolvedValue([[makeIom()], 1]),
     } as unknown as jest.Mocked<SelectQueryBuilder<Iom>>;
 
@@ -143,24 +145,87 @@ describe('IomListingService', () => {
     await service.findIoms(CRM_USER, { search: 'jane' });
 
     expect(qb.andWhere).toHaveBeenCalledWith(
-      '(booking.customerName LIKE :like OR booking.bookingId LIKE :like OR booking.propertyNumber LIKE :like OR project.name LIKE :like)',
+      '(booking.customerName LIKE :like OR booking.bookingId LIKE :like OR booking.propertyNumber LIKE :like OR project.name LIKE :like OR i.iomNo LIKE :like)',
       { like: '%jane%' },
     );
   });
 
-  it('throws BadRequestException for invalid status', async () => {
+  it('throws BadRequestException for invalid iomStatus', async () => {
     await expect(
-      service.findIoms(CRM_USER, { status: 'NOT_A_STATUS' }),
+      service.findIoms(CRM_USER, {
+        iomStatus: 'NOT_A_STATUS',
+      } as unknown as ListIomListingDto),
     ).rejects.toBeInstanceOf(BadRequestException);
     expect(qb.getManyAndCount).not.toHaveBeenCalled();
   });
 
-  it('filters by valid status code', async () => {
-    await service.findIoms(CRM_USER, { status: IomStatusCodeEnum.IOM_CREATED });
+  it('filters by valid iomStatus code', async () => {
+    await service.findIoms(CRM_USER, {
+      iomStatus: IomStatusCodeEnum.IOM_CREATED,
+    } as unknown as ListIomListingDto);
 
-    expect(qb.andWhere).toHaveBeenCalledWith('status.code = :status', {
-      status: IomStatusCodeEnum.IOM_CREATED,
-    });
+    expect(qb.andWhere).toHaveBeenCalledWith(
+      'status.code IN (:...effectiveStatuses)',
+      {
+        effectiveStatuses: [IomStatusCodeEnum.IOM_CREATED],
+      },
+    );
+  });
+
+  it('applies CRM_HEAD role status bucket filter', async () => {
+    const crmHeadUser: AuthenticatedUser = {
+      ...CRM_USER,
+      role: RolesEnum.CRM_HEAD,
+    };
+
+    await service.findIoms(crmHeadUser, {});
+
+    expect(qb.andWhere).toHaveBeenCalledWith(
+      'status.code IN (:...effectiveStatuses)',
+      expect.objectContaining({
+        effectiveStatuses: expect.arrayContaining([
+          IomStatusCodeEnum.TL_APPROVED,
+          IomStatusCodeEnum.CRM_HEAD_REJECTED,
+        ]),
+      }),
+    );
+  });
+
+  it('applies LOYALTY role status bucket filter', async () => {
+    const loyaltyUser: AuthenticatedUser = {
+      ...CRM_USER,
+      role: RolesEnum.LOYALTY,
+    };
+
+    await service.findIoms(loyaltyUser, {});
+
+    expect(qb.andWhere).toHaveBeenCalledWith(
+      'status.code IN (:...effectiveStatuses)',
+      {
+        effectiveStatuses: [
+          IomStatusCodeEnum.POINTS_TO_BE_UPLOADED,
+          IomStatusCodeEnum.POINTS_UPLOADED,
+          IomStatusCodeEnum.INVOICE_SUBMITTED,
+          IomStatusCodeEnum.INVOICE_REQUESTED,
+          IomStatusCodeEnum.IOM_CLOSED,
+        ],
+      },
+    );
+  });
+
+  it('findAllForExport applies role filter without pagination', async () => {
+    const loyaltyUser: AuthenticatedUser = {
+      ...CRM_USER,
+      role: RolesEnum.LOYALTY,
+    };
+
+    const items = await service.findAllForExport(loyaltyUser);
+
+    expect(qb.skip).not.toHaveBeenCalled();
+    expect(qb.take).not.toHaveBeenCalled();
+    expect(qb.getMany).toHaveBeenCalled();
+    expect(items).toHaveLength(1);
+    expect(items[0].projectName).toBe('Tower One');
   });
 
   it('applies date range on createdAt', async () => {
@@ -205,7 +270,7 @@ describe('IomListingService', () => {
     const result = await service.findIoms(CRM_USER, { page: 1, limit: 10 });
 
     expect(result.totalPages).toBe(1);
-    expect(result.items[0]).toEqual({
+    expect(result.items[0]).toMatchObject({
       id: 1,
       bookingId: 100,
       projectId: 10,
@@ -221,7 +286,7 @@ describe('IomListingService', () => {
       referralClassification: 'CLASS_A',
       statusCode: IomStatusCodeEnum.IOM_CREATED,
       statusLabel: 'IOM Created',
-      submittedAt: null,
+      iomCreatedAt: null,
       createdAt: new Date('2026-01-15T10:00:00Z'),
       iomPdfAvailable: true,
     });
diff --git a/src/modules/iom/services/iom-listing.service.ts b/src/modules/iom/services/iom-listing.service.ts
index dbd8156f..237f6028 100644
--- a/src/modules/iom/services/iom-listing.service.ts
+++ b/src/modules/iom/services/iom-listing.service.ts
@@ -1,6 +1,6 @@
 import { BadRequestException, Injectable } from '@nestjs/common';
 import { InjectRepository } from '@nestjs/typeorm';
-import { IsNull, Repository } from 'typeorm';
+import { IsNull, Repository, SelectQueryBuilder } from 'typeorm';
 
 import { DEFAULT_LIMIT, DEFAULT_PAGE } from 'src/config/constants';
 import { ProjectUserMapping } from 'src/entities';
@@ -13,6 +13,7 @@ import {
 } from '../types/iom-list-item.interface';
 import { AuthenticatedUser } from './iom-validation.service';
 import { toTitleCase } from 'src/helpers/stringHelper';
+import { getAllowedIomStatusesByRole } from '../utils/iom-role-status.util';
 
 const ALLOWED_SORT_FIELDS: Record<string, string> = {
   createdAt: 'i.createdAt',
@@ -45,17 +46,17 @@ export class IomListingService {
       return { items: [], total: 0, page, limit, totalPages: 0 };
     }
 
-    let iomStatuses: IomStatusCodeEnum[] | undefined;
+    let queryStatuses: IomStatusCodeEnum[] | undefined;
 
     if (query.iomStatus) {
       const validStatuses = Object.values(IomStatusCodeEnum);
 
-      iomStatuses = query.iomStatus
+      queryStatuses = query.iomStatus
         .split(',')
         .map((s) => s.trim())
         .filter(Boolean) as IomStatusCodeEnum[];
 
-      const invalid = iomStatuses.filter((s) => !validStatuses.includes(s));
+      const invalid = queryStatuses.filter((s) => !validStatuses.includes(s));
 
       if (invalid.length) {
         throw new BadRequestException(
@@ -63,7 +64,96 @@ export class IomListingService {
         );
       }
     }
-    const qb = this.iomRepo
+
+    const effectiveStatuses = this.resolveEffectiveStatuses(
+      user.role,
+      queryStatuses,
+    );
+
+    const qb = this.createBaseQueryBuilder(crmProjects);
+    this.applyListingFilters(qb, query, effectiveStatuses);
+
+    if (query.sortBy) {
+      const [field, direction] = query.sortBy.split(':');
+      const dir = (direction || '').toUpperCase() === 'DESC' ? 'DESC' : 'ASC';
+      const column = ALLOWED_SORT_FIELDS[field];
+      if (column) {
+        qb.orderBy(column, dir as 'ASC' | 'DESC');
+      }
+    } else {
+      qb.orderBy('i.createdAt', 'DESC');
+    }
+
+    qb.skip(skip).take(limit);
+
+    const [rows, total] = await qb.getManyAndCount();
+
+    return {
+      items: rows.map((iom) => this.toListItem(iom)),
+      total,
+      page,
+      limit,
+      totalPages: Math.ceil(total / limit),
+    };
+  }
+
+  async findAllForExport(user: AuthenticatedUser): Promise<IomListItem[]> {
+    const crmProjects = await this.resolveUserProjects(user);
+
+    if (crmProjects.length === 0) {
+      return [];
+    }
+
+    const effectiveStatuses = this.resolveEffectiveStatuses(user.role);
+    const qb = this.createBaseQueryBuilder(crmProjects);
+
+    if (effectiveStatuses?.length) {
+      qb.andWhere('status.code IN (:...effectiveStatuses)', {
+        effectiveStatuses,
+      });
+    }
+
+    qb.orderBy('i.createdAt', 'DESC');
+
+    const rows = await qb.getMany();
+    return rows.map((iom) => this.toListItem(iom));
+  }
+
+  private resolveEffectiveStatuses(
+    userRole: string,
+    queryStatuses?: IomStatusCodeEnum[],
+  ): IomStatusCodeEnum[] | undefined {
+    const roleStatuses = getAllowedIomStatusesByRole(userRole);
+
+    if (!queryStatuses?.length) {
+      return roleStatuses;
+    }
+
+    if (!roleStatuses) {
+      return queryStatuses;
+    }
+
+    const allowedSet = new Set(roleStatuses);
+    const intersection = queryStatuses.filter((status) =>
+      allowedSet.has(status),
+    );
+
+    const disallowed = queryStatuses.filter(
+      (status) => !allowedSet.has(status),
+    );
+    if (disallowed.length) {
+      throw new BadRequestException(
+        `iomStatus values not allowed for your role: ${disallowed.join(', ')}`,
+      );
+    }
+
+    return intersection;
+  }
+
+  private createBaseQueryBuilder(
+    crmProjects: number[],
+  ): SelectQueryBuilder<Iom> {
+    return this.iomRepo
       .createQueryBuilder('i')
       .innerJoinAndSelect('i.status', 'status')
       .innerJoinAndSelect('i.booking', 'booking')
@@ -78,7 +168,13 @@ export class IomListingService {
       .leftJoinAndSelect('inv.createdByUser', 'invoiceCreator')
       .leftJoinAndSelect('inv.updatedByUser', 'invoiceUpdater')
       .where('i.projectId IN (:...crmProjects)', { crmProjects });
+  }
 
+  private applyListingFilters(
+    qb: SelectQueryBuilder<Iom>,
+    query: ListIomListingDto,
+    effectiveStatuses?: IomStatusCodeEnum[],
+  ): void {
     if (query.search) {
       const like = `%${query.search}%`;
       qb.andWhere(
@@ -87,9 +183,10 @@ export class IomListingService {
       );
     }
 
-    console.log('iom stauts filter is getting applied', query);
-    if (query.iomStatus && iomStatuses?.length) {
-      qb.andWhere('status.code IN (:...iomStatuses)', { iomStatuses });
+    if (effectiveStatuses?.length) {
+      qb.andWhere('status.code IN (:...effectiveStatuses)', {
+        effectiveStatuses,
+      });
     }
 
     if (query.startDate) {
@@ -105,29 +202,6 @@ export class IomListingService {
         invoiceStatus: query.invoiceStatus,
       });
     }
-
-    if (query.sortBy) {
-      const [field, direction] = query.sortBy.split(':');
-      const dir = (direction || '').toUpperCase() === 'DESC' ? 'DESC' : 'ASC';
-      const column = ALLOWED_SORT_FIELDS[field];
-      if (column) {
-        qb.orderBy(column, dir as 'ASC' | 'DESC');
-      }
-    } else {
-      qb.orderBy('i.createdAt', 'DESC');
-    }
-
-    qb.skip(skip).take(limit);
-
-    const [rows, total] = await qb.getManyAndCount();
-
-    return {
-      items: rows.map((iom) => this.toListItem(iom)),
-      total,
-      page,
-      limit,
-      totalPages: Math.ceil(total / limit),
-    };
   }
 
   /**
diff --git a/src/modules/users/services/user-availability.service.spec.ts b/src/modules/users/services/user-availability.service.spec.ts
index cfd85047..74c14ddf 100644
--- a/src/modules/users/services/user-availability.service.spec.ts
+++ b/src/modules/users/services/user-availability.service.spec.ts
@@ -16,16 +16,11 @@ import { Users } from '../entities/user.entity';
 import { AwsService } from 'src/modules/aws/aws.service';
 import { CustomConfigService } from 'src/config/custom-config.service';
 import { generateExcelBuffer } from 'src/common/helpers/excel.helper';
-import { uploadBufferToS3 } from 'src/common/helpers/s3-upload.helper';
 
 jest.mock('src/common/helpers/excel.helper', () => ({
   generateExcelBuffer: jest.fn(),
 }));
 
-jest.mock('src/common/helpers/s3-upload.helper', () => ({
-  uploadBufferToS3: jest.fn(),
-}));
-
 jest.mock('src/helpers/date.helper', () => ({
   formatDateUtil: jest.fn().mockReturnValue('17Jun_3-30PM'),
 }));
@@ -33,6 +28,8 @@ jest.mock('src/helpers/date.helper', () => ({
 describe('UserAvailabilityService', () => {
   let service: UserAvailabilityService;
   let userService: { validateTLAccess: jest.Mock };
+  let awsService: { uploadToS3: jest.Mock };
+  let configService: { get: jest.Mock };
   let availabilityRepo: {
     createQueryBuilder: jest.Mock;
     save: jest.Mock;
@@ -191,6 +188,8 @@ describe('UserAvailabilityService', () => {
     }).compile();
 
     service = module.get(UserAvailabilityService);
+    awsService = module.get(AwsService);
+    configService = module.get(CustomConfigService);
   });
 
   afterEach(() => jest.clearAllMocks());
@@ -693,9 +692,8 @@ describe('UserAvailabilityService', () => {
       (generateExcelBuffer as jest.Mock).mockResolvedValue(
         Buffer.from('excel'),
       );
-      (uploadBufferToS3 as jest.Mock).mockResolvedValue(
-        'https://cdn.example.com/puravankara/exports/team-availability-17Jun_3-30PM.xlsx',
-      );
+      awsService.uploadToS3.mockResolvedValue(undefined);
+      configService.get.mockReturnValue('https://cdn.example.com/puravankara/');
     });
 
     it('exports all filtered rows and returns the S3 URL', async () => {
@@ -734,17 +732,16 @@ describe('UserAvailabilityService', () => {
         ],
         'Team Availability',
       );
-      expect(uploadBufferToS3).toHaveBeenCalledWith(
-        expect.anything(),
-        expect.anything(),
-        expect.any(Buffer),
+      expect(awsService.uploadToS3).toHaveBeenCalledWith(
         'exports/team-availability-17Jun_3-30PM.xlsx',
-        'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
+        expect.anything(),
+        true,
       );
       expect(result).toEqual({
         message: 'Team availability exported successfully',
         data: {
-          url: 'https://cdn.example.com/puravankara/exports/team-availability-17Jun_3-30PM.xlsx',
+          url: 'exports/team-availability-17Jun_3-30PM.xlsx',
+          basePath: 'https://cdn.example.com/puravankara/',
         },
       });
     });
diff --git a/src/modules/users/services/user-availability.service.ts b/src/modules/users/services/user-availability.service.ts
index 2fa51080..25c50d0d 100644
--- a/src/modules/users/services/user-availability.service.ts
+++ b/src/modules/users/services/user-availability.service.ts
@@ -21,7 +21,7 @@ import { Users } from '../entities/user.entity';
 import { Iom, ProjectUserMapping } from 'src/entities';
 import { RolesEnum } from 'src/enums/roles.enum';
 import { toTitleCase } from 'src/helpers';
-import { formatDateTime, formatDateUtil } from 'src/helpers/date.helper';
+import { formatDateUtil } from 'src/helpers/date.helper';
 import { generateExcelBuffer } from 'src/common/helpers/excel.helper';
 import { AwsService } from 'src/modules/aws/aws.service';
 import { CustomConfigService } from 'src/config/custom-config.service';
@@ -75,17 +75,16 @@ export class UserAvailabilityService {
       );
     }
 
-    const hasActiveUnavailability = await this.availabilityRepo
+    const overlapping = await this.availabilityRepo
       .createQueryBuilder('ua')
       .where('ua.userId = :userId', { userId: dto.userId })
-      .andWhere('ua.cancelled_at IS NULL')
-      // .andWhere('ua.unavailableFrom < :to', { to: unavailableTo })
-      .andWhere('ua.unavailableTo >= :now', { now: new Date() })
+      .andWhere('ua.unavailableFrom < :to', { to: unavailableTo })
+      .andWhere('ua.unavailableTo > :from', { from: unavailableFrom })
       .getOne();
 
-    if (hasActiveUnavailability) {
+    if (overlapping) {
       throw new ConflictException(
-        'User already has an active unavailability',
+        'An overlapping unavailability window already exists for this user',
       );
     }
 
@@ -195,9 +194,9 @@ export class UserAvailabilityService {
     if (query.search) {
       const like = `%${query.search}%`;
       qb.andWhere(
-        `(u.name LIKE :like 
-          OR u.email LIKE :like 
-          OR u.empCode LIKE :like)`,
+        `(u.name ILIKE :like 
+          OR u.email ILIKE :like 
+          OR u.empCode ILIKE :like)`,
         { like },
       );
     }
@@ -232,7 +231,7 @@ export class UserAvailabilityService {
     const userIds = teamMembers.map((member) => member.id);
 
     const [activeWindows, projectMappings, iomCounts] = await Promise.all([
-      this.loadRelevantWindowsForUsers(userIds, now),
+      this.loadActiveWindowsForUsers(userIds, now),
       this.loadProjectsForUsers(userIds),
       this.loadAllocatedIomCounts(userIds),
     ]);
@@ -293,9 +292,9 @@ export class UserAvailabilityService {
         fromDateTime:
           item.currentStatus === 'AVAILABLE'
             ? ''
-            : (formatDateTime( item.unavailableFrom) ?? ''),
+            : (item.unavailableFrom ?? ''),
         toDateTime:
-          item.currentStatus === 'AVAILABLE' ? '' : (formatDateTime( item.unavailableTo) ?? ''),
+          item.currentStatus === 'AVAILABLE' ? '' : (item.unavailableTo ?? ''),
       }));
 
       const buffer = await generateExcelBuffer(
@@ -337,35 +336,26 @@ export class UserAvailabilityService {
       .getOne();
   }
 
-  private async loadRelevantWindowsForUsers(
+  private async loadActiveWindowsForUsers(
     userIds: number[],
     now: Date,
   ): Promise<Map<number, UserAvailability>> {
     const rows = await this.availabilityRepo
       .createQueryBuilder('ua')
       .where('ua.user_id IN (:...userIds)', { userIds })
-      .andWhere('ua.cancelled_at IS NULL')
-      .andWhere('ua.unavailable_to >= :now', { now }) // active OR upcoming
-      .orderBy(
-        `
-        CASE 
-          WHEN ua.unavailable_from <= :now THEN 0  -- active first
-          ELSE 1                                   -- upcoming next
-        END,
-        ua.unavailable_from `,
-        `ASC`
-      )
-      .setParameter('now', now)
+      .andWhere('ua.unavailable_from <= :now', { now })
+      .andWhere('ua.unavailable_to >= :now', { now })
+      .orderBy('ua.unavailable_from', 'DESC')
       .getMany();
 
-    const map = new Map<number, UserAvailability>();
-    for (const row of rows) {
-      if (!map.has(row.userId)) {
-        map.set(row.userId, row);
+    const activeByUser = new Map<number, UserAvailability>();
+    for (const window of rows) {
+      if (!activeByUser.has(window.userId)) {
+        activeByUser.set(window.userId, window);
       }
     }
 
-    return map;
+    return activeByUser;
   }
 
   private async loadProjectsForUsers(

## Staged Diff

diff --git a/src/common/helpers/excel.helper.ts b/src/common/helpers/excel.helper.ts
index 2459c868..40e812d8 100644
--- a/src/common/helpers/excel.helper.ts
+++ b/src/common/helpers/excel.helper.ts
@@ -6,9 +6,10 @@ const MAX_COLUMN_WIDTH = 50;
 export async function generateExcelBuffer<T extends Record<string, unknown>>(
   columns: { header: string; key: string; width?: number }[],
   data: T[],
+  worksheetName = 'Sheet1',
 ): Promise<Buffer> {
   const workbook = new ExcelJS.Workbook();
-  const worksheet = workbook.addWorksheet('Team Availability');
+  const worksheet = workbook.addWorksheet(worksheetName);
 
   worksheet.columns = columns.map((column) => ({
     header: column.header,
diff --git a/src/modules/users/services/user-availability.service.spec.ts b/src/modules/users/services/user-availability.service.spec.ts
index 261c4a27..cfd85047 100644
--- a/src/modules/users/services/user-availability.service.spec.ts
+++ b/src/modules/users/services/user-availability.service.spec.ts
@@ -732,6 +732,7 @@ describe('UserAvailabilityService', () => {
             toDateTime: '',
           }),
         ],
+        'Team Availability',
       );
       expect(uploadBufferToS3).toHaveBeenCalledWith(
         expect.anything(),
@@ -763,12 +764,16 @@ describe('UserAvailabilityService', () => {
 
       await service.exportTeamAvailability(tlUser, listQuery);
 
-      expect(generateExcelBuffer).toHaveBeenCalledWith(expect.any(Array), [
-        expect.objectContaining({
-          fromDateTime: unavailableFrom.toISOString(),
-          toDateTime: unavailableTo.toISOString(),
-        }),
-      ]);
+      expect(generateExcelBuffer).toHaveBeenCalledWith(
+        expect.any(Array),
+        [
+          expect.objectContaining({
+            fromDateTime: unavailableFrom.toISOString(),
+            toDateTime: unavailableTo.toISOString(),
+          }),
+        ],
+        'Team Availability',
+      );
     });
 
     it('throws InternalServerErrorException when export fails', async () => {
diff --git a/src/modules/users/services/user-availability.service.ts b/src/modules/users/services/user-availability.service.ts
index 04f1b297..2fa51080 100644
--- a/src/modules/users/services/user-availability.service.ts
+++ b/src/modules/users/services/user-availability.service.ts
@@ -21,7 +21,7 @@ import { Users } from '../entities/user.entity';
 import { Iom, ProjectUserMapping } from 'src/entities';
 import { RolesEnum } from 'src/enums/roles.enum';
 import { toTitleCase } from 'src/helpers';
-import { formatDateUtil } from 'src/helpers/date.helper';
+import { formatDateTime, formatDateUtil } from 'src/helpers/date.helper';
 import { generateExcelBuffer } from 'src/common/helpers/excel.helper';
 import { AwsService } from 'src/modules/aws/aws.service';
 import { CustomConfigService } from 'src/config/custom-config.service';
@@ -75,16 +75,17 @@ export class UserAvailabilityService {
       );
     }
 
-    const overlapping = await this.availabilityRepo
+    const hasActiveUnavailability = await this.availabilityRepo
       .createQueryBuilder('ua')
       .where('ua.userId = :userId', { userId: dto.userId })
-      .andWhere('ua.unavailableFrom < :to', { to: unavailableTo })
-      .andWhere('ua.unavailableTo > :from', { from: unavailableFrom })
+      .andWhere('ua.cancelled_at IS NULL')
+      // .andWhere('ua.unavailableFrom < :to', { to: unavailableTo })
+      .andWhere('ua.unavailableTo >= :now', { now: new Date() })
       .getOne();
 
-    if (overlapping) {
+    if (hasActiveUnavailability) {
       throw new ConflictException(
-        'An overlapping unavailability window already exists for this user',
+        'User already has an active unavailability',
       );
     }
 
@@ -194,9 +195,9 @@ export class UserAvailabilityService {
     if (query.search) {
       const like = `%${query.search}%`;
       qb.andWhere(
-        `(u.name ILIKE :like 
-          OR u.email ILIKE :like 
-          OR u.empCode ILIKE :like)`,
+        `(u.name LIKE :like 
+          OR u.email LIKE :like 
+          OR u.empCode LIKE :like)`,
         { like },
       );
     }
@@ -231,7 +232,7 @@ export class UserAvailabilityService {
     const userIds = teamMembers.map((member) => member.id);
 
     const [activeWindows, projectMappings, iomCounts] = await Promise.all([
-      this.loadActiveWindowsForUsers(userIds, now),
+      this.loadRelevantWindowsForUsers(userIds, now),
       this.loadProjectsForUsers(userIds),
       this.loadAllocatedIomCounts(userIds),
     ]);
@@ -292,14 +293,15 @@ export class UserAvailabilityService {
         fromDateTime:
           item.currentStatus === 'AVAILABLE'
             ? ''
-            : (item.unavailableFrom ?? ''),
+            : (formatDateTime( item.unavailableFrom) ?? ''),
         toDateTime:
-          item.currentStatus === 'AVAILABLE' ? '' : (item.unavailableTo ?? ''),
+          item.currentStatus === 'AVAILABLE' ? '' : (formatDateTime( item.unavailableTo) ?? ''),
       }));
 
       const buffer = await generateExcelBuffer(
         TEAM_AVAILABILITY_EXPORT_COLUMNS,
         rows,
+        'Team Availability',
       );
 
       const timestamp = formatDateUtil(undefined, 'timestamp');
@@ -335,26 +337,35 @@ export class UserAvailabilityService {
       .getOne();
   }
 
-  private async loadActiveWindowsForUsers(
+  private async loadRelevantWindowsForUsers(
     userIds: number[],
     now: Date,
   ): Promise<Map<number, UserAvailability>> {
     const rows = await this.availabilityRepo
       .createQueryBuilder('ua')
       .where('ua.user_id IN (:...userIds)', { userIds })
-      .andWhere('ua.unavailable_from <= :now', { now })
-      .andWhere('ua.unavailable_to >= :now', { now })
-      .orderBy('ua.unavailable_from', 'DESC')
+      .andWhere('ua.cancelled_at IS NULL')
+      .andWhere('ua.unavailable_to >= :now', { now }) // active OR upcoming
+      .orderBy(
+        `
+        CASE 
+          WHEN ua.unavailable_from <= :now THEN 0  -- active first
+          ELSE 1                                   -- upcoming next
+        END,
+        ua.unavailable_from `,
+        `ASC`
+      )
+      .setParameter('now', now)
       .getMany();
 
-    const activeByUser = new Map<number, UserAvailability>();
-    for (const window of rows) {
-      if (!activeByUser.has(window.userId)) {
-        activeByUser.set(window.userId, window);
+    const map = new Map<number, UserAvailability>();
+    for (const row of rows) {
+      if (!map.has(row.userId)) {
+        map.set(row.userId, row);
       }
     }
 
-    return activeByUser;
+    return map;
   }
 
   private async loadProjectsForUsers(
